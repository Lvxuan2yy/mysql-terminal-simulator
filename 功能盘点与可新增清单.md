# MySQL 终端模拟器 · 能力盘点与可新增功能清单

> 编写时间：2026-09-21
> 核对对象：`mysql-terminal.html`（1,102,884 字节 · SHA-256 `16378e5c…d6bddd3`，与交接文档 §1 完全一致）
> 目的：说清"现在能做什么 / 按设计不做什么 / 不能做但**值得新增**什么"，并登记本次新发现的缺陷。

---

## ⚠️ 实施状态（2026-09-21 更新）

本文档的 **§3 A 档与 B 档已全部实施**，**§4 里所有零风险缺陷已修复**。下面保留原始的盘点内容作为"问题档案"（每条都标了根因与修法），实际实施结果与验证情况见 `交接文档.md` §13。

| 范围 | 状态 |
|---|---|
| §3.1 A1–A12（12 项） | ✅ 全部完成 |
| §3.2 B1（跨库限定名） | ⏸ **暂缓**：可行性已实测确认（`ATTACH` 可用），但属架构级改造，风险高；已实现低风险部分（限定到当前库的 `db.tbl`）并把限制如实写入 `DIFFERENCES`/README |
| §3.2 B2–B12（11 项） | ✅ 全部完成 |
| §3.2 B13（Web Worker） | ⏸ **暂缓**：会动摇"离线单文件 + 零外部请求"这条不变量，收益与风险不成比例 |
| §3.3 C1–C7（大工程） | 未动，仍待产品决策 |
| §4 P0-1 ~ P2-8 | ✅ 全部修复，每条都有 `test-fixes.cjs` 断言守着 |

**实施后的验证基线**：核心层 / 视图 / 练习题 46 题 / `test-fixes.cjs` 167 条断言 / 缺口普查（**意外语法错 0**）/ Edge 无头浏览器 26 项，全部通过。成品指纹：1,206,740 字节 · SHA-256 `4f38f83f…`。

---

## 0. 核对方式（可复现）

| 步骤 | 做法 | 结果 |
|---|---|---|
| 读文档 | `README.md`、`交接文档.md`、`THIRD-PARTY-NOTICES.md`、`.gitignore` | 已读，内容与代码基本一致（差异见 §5） |
| 读源码 | `src/mysql-core.js`（2458 行）全节 + `src/app.js` / `template.html` / `build.py` / 测试脚本 | 已读 |
| 跑基线 | `node test-core.cjs` → `failures=0`；`test-view.cjs` → `failures=0`；`test-exercise.cjs` → `failures=0`；`probe-gaps.cjs` → 通过 112 / 预期不支持 13 / 意外 1064 = **0** | **与交接文档 §8 声明一致，基线可信** |
| 核指纹 | `Get-FileHash mysql-terminal.html` | 大小与 SHA-256 与文档逐字符一致 ✅ |
| 补探测 | 另写 3 组临时探针，共跑 **211 条**文档未覆盖的语句（跑完即删，未留在工作区） | 本文所有标"实测"的结论均来自这些探针 |

> ⚠️ 口径提醒：`probe-gaps.cjs` 的"意外 1064 = 0"**只对它自己那 112 条用例成立**。本文 §4 补测的语句里有一批仍然报 1064（如 `DATE_ADD(..., INTERVAL 1 MONTH)`）。这个基线数字容易让人误以为"语法覆盖已无死角"，建议在文档里写明口径（详见 §6）。

---

## 1. 已经能做的事（实测确认）

### 1.1 引擎与形态

| 能力 | 说明 | 代码落点 |
|---|---|---|
| 零依赖单文件 | 引擎 + 数据 + 界面全内联，双击即用、不联网、不上传 | `build.py` 内联 wasm(base64) |
| MySQL 方言兼容层 | 不用改写 SQL，MySQL 写法直接跑 | `translateStatement` `mysql-core.js:449` |
| MySQL 风格排版 | CJK 全角宽度对齐、`+---+` 表格、`\G` 纵向、`N rows in set (0.00 sec)` | `dispWidth:34` / `formatTable:574` / `formatVertical:612` |
| MySQL 风格错误码 | 1146 / 1054 / 1050 / 1062 / 1048 / 1305 / 1288 / 1347 / 1193 … | `mapError:638` |
| 真实事务 | `START TRANSACTION`/`BEGIN`/`COMMIT`/`ROLLBACK`/`SAVEPOINT`/`ROLLBACK TO`，**回滚真的生效** | `runOne:1471` |
| 启动自检 | 文件被截断/内核过旧时有友好提示，不白屏 | `template.html:17` |
| 可移植 | 中文路径、断网、禁 localStorage、老内核、chromium/firefox/webkit 均通过 | `src/tests/test-portable.py` 等 |

### 1.2 DDL

- `CREATE/DROP DATABASE`（含 `IF NOT EXISTS`/`IF EXISTS`）、`ALTER DATABASE`
- `CREATE TABLE`：完整列定义解析、`IF NOT EXISTS`、`CREATE TABLE ... LIKE`、`CREATE TABLE ... AS SELECT`（可跑，但类型元数据有损，见 §4 P2-5）
- `DROP TABLE`（支持一次删多张）、`TRUNCATE TABLE`、`RENAME TABLE`
- `ALTER TABLE`：`ADD/DROP/MODIFY/CHANGE/RENAME COLUMN`、`ADD/DROP INDEX|KEY`、`ADD UNIQUE KEY`、`RENAME TO`
- 类型转译：`AUTO_INCREMENT`、`ENGINE=`/`DEFAULT CHARSET=`/`COLLATE=`/`COMMENT=`、`ENUM`/`SET`/`JSON`→TEXT、`UNSIGNED`/`ZEROFILL`、反引号
- 建表元数据自建目录（`parseCreateTable:209`），所以 `DESC` / `SHOW CREATE TABLE` / `SHOW INDEX` 能给出 MySQL 形态结果

### 1.3 查询

- 多表 `JOIN`（LEFT/RIGHT/INNER/CROSS）、`GROUP BY`/`HAVING`、`ORDER BY`、`LIMIT a,b` / `LIMIT n OFFSET m`、`DISTINCT`
- 子查询（标量/IN/EXISTS/派生表）、`UNION`、`CASE WHEN`、`IN`/`BETWEEN`/`LIKE`/`RLIKE`/`REGEXP`
- 窗口函数：`RANK`/`ROW_NUMBER`/`DENSE_RANK`/`LAG`/`LEAD`/`SUM() OVER`/`NTILE` ✅ 实测可用
- CTE：`WITH ...` 与 `WITH RECURSIVE` ✅ 实测可用（文档未宣传，属于"白捡的能力"）
- 聚合：`COUNT/SUM/AVG/MAX/MIN`、`COUNT(DISTINCT)`、`GROUP_CONCAT`
- JSON：`JSON_EXTRACT`/`JSON_OBJECT`/`JSON_ARRAY`/`->` ✅ 可用（SQLite 内建）
- `EXPLAIN`（输出形态是 SQLite 的，见 §4 P2-7）
- 变量：`SET @x=1`、`SELECT @x`、`@@version`/`@@port` 等、未赋值返回 NULL（与 MySQL 一致）

### 1.4 DML

`INSERT`、`INSERT IGNORE`、`INSERT ... SELECT`、`INSERT ... ON DUPLICATE KEY UPDATE`（实测可用）、`REPLACE INTO`、`UPDATE`（含 `LIMIT`/`ORDER BY`）、`DELETE`（含 `LIMIT`/`ORDER BY`）

### 1.5 元数据 / SHOW

`SHOW DATABASES|SCHEMAS`、`SHOW TABLES [FROM db] [LIKE]`、`SHOW FULL TABLES`、`SHOW COLUMNS|FIELDS [FROM db]`、`SHOW CREATE TABLE|VIEW|DATABASE`、`SHOW INDEX|KEYS`、`SHOW VARIABLES [LIKE]`、`SHOW STATUS`、`SHOW ENGINES`、`SHOW TABLE STATUS`、`SHOW PROCESSLIST`、`SHOW WARNINGS|ERRORS`、`SHOW CHARSET`、`SHOW COLLATION`、`SHOW GRANTS`、`DESC`/`DESCRIBE`、`CHECK TABLE`

### 1.6 视图（完整支持）

`CREATE VIEW` / `CREATE OR REPLACE VIEW` / `ALTER VIEW` / `DROP VIEW`；建视图时**主动校验引用**（少了表/列立刻报 1146/1054，不留半成品）；`SHOW TABLES`/`SHOW FULL TABLES`/`DESC`/`SHOW CREATE VIEW` 全部识别；侧栏用 `◇` 紫色标记。

### 1.7 客户端命令与学习辅助

`help`/`\h`、`status`/`\s`、`clear`/`Ctrl+L`、`exit`/`quit`/`Ctrl+D`、`\G` 纵向输出、`DELIMITER`（接受并忽略）、`LOCK/UNLOCK TABLES`（接受并提示无锁语义）；`↑↓` 历史、`Tab` 补全、`Ctrl+C` 放弃；**24 道练习题**自动判分（忽略大小写/空格/反引号/单字母别名，按整条语句比对防串台）；「差异」面板渲染 `DIFFERENCES` 诚实清单。

---

## 2. 按设计不做，并且**有诚实报错**的（不建议改）

这些是刻意决策，报错信息里给了真实原因和替代方案，属于项目卖点，动了反而变差：

| 不做的事 | 行为 |
|---|---|
| 存储过程 / 函数 / 触发器 / 事件 | `ERROR 1235` + 说明 |
| `PREPARE` / `EXECUTE` / `DEALLOCATE` | `ERROR 1235` |
| `GRANT` / `REVOKE` / `CREATE USER` | `ERROR 1235`（一律以 root@localhost 执行） |
| `LOAD DATA` / `SELECT ... INTO OUTFILE` | `ERROR 1235`（浏览器无文件系统） |
| `UPDATE/DELETE ... JOIN`（多表写） | `ERROR 1235` + 建议改写成 `WHERE EXISTS` |
| `GROUP BY ... WITH ROLLUP` | `ERROR 1235` |
| `ALTER TABLE ... ADD PRIMARY KEY` | `ERROR 1235`（SQLite 需重建整表） |
| 透过视图写数据 | `ERROR 1288 ... is not updatable` |
| `BACKUP DATABASE` / `RESTORE` | **刻意报 1064** —— MySQL 社区版本身没有这条 SQL（企业版组件），这个考据是对的，值得保留 |
| `mysqldump` | 同上，`1064` + 说明它是 shell 命令 |

---

## 3. 不能做，但**可以新增**的功能

按"性价比"分三档。每项给出价值 / 落点 / 做法。

### 3.1 A 档：低成本、高收益（核心层几处小改，1–2 天量级）

| # | 功能 | 价值 | 落点与做法 |
|---|---|---|---|
| A1 | **把"假 1064"清零**：补登记 `ALTER TABLE ... ENGINE=/AUTO_INCREMENT=/DROP PRIMARY KEY/DROP FOREIGN KEY/ADD CONSTRAINT`、`PARTITION BY`、`FULLTEXT/SPATIAL INDEX`、`MATCH ... AGAINST` | 直接强化"诚实"这个核心卖点 | `UNSUPPORTED_STATEMENTS`（`mysql-core.js:989`）加正则条目，照抄现有 `ADD PRIMARY KEY` 的写法 |
| A2 | **`SHOW` 家族补空结果集**：`SHOW TRIGGERS|EVENTS|PROCEDURE STATUS|FUNCTION STATUS|OPEN TABLES|BINARY LOGS|ENGINE INNODB STATUS`、`SHOW COUNT(*) WARNINGS` | 这些在真 MySQL 里就是空集或简短状态，成本极低 | `handleShow`（`:1975`）加分支返回 `rs([...], [])` |
| A3 | **修 `SHOW`/`DESC` 的 LIKE 过滤**（见 §4 P1-1） | 修掉"过滤条件被无视"的错答 | `handleShow:1981`（DATABASES 缺 LIKE）、`:1997`（COLUMNS 丢 LIKE/列名） |
| A4 | **修 `SHOW INDEX` 对无索引表谎报 1146**（§4 P1-2） | 错答 | `:2035` 改成返回空结果集 |
| A5 | **修表维护语句输出形态**（§4 P1-4） | `OPTIMIZE TABLE` 现在输出 `Empty set`，很扎眼 | `:536` 的 `SELECT 1 WHERE 0` 换成走 `CHECK TABLE` 那条真实现（`:1502`） |
| A6 | **修变参函数注册**（§4 P0-1）：`GREATEST`/`LEAST` 完全不可用、`CONCAT` 语义与 MySQL 相反 | README 宣称的 48 个函数里 5 个中招 | `def()`（`:761`）：按元数 1..N 分别注册包装函数，或绕过 sql.js 直接调 `sqlite3_create_function_v2`（`nArg=-1`） |
| A7 | **补一批标量函数**（约 25 个）：`DATEDIFF`、`TIMESTAMPDIFF`、`DAYNAME`、`MONTHNAME`、`WEEK`、`QUARTER`、`DAYOFYEAR`、`STR_TO_DATE`、`SUBSTRING_INDEX`、`ELT`、`FIND_IN_SET`、`ASCII`、`CHAR`、`HEX`、`MD5`、`SHA1`、`CONV`、`MOD`、`POW`、`TRUNCATE`、`STDDEV`、`VARIANCE`、`LAST_INSERT_ID`、`ROW_COUNT`、`FOUND_ROWS` | 纯增量，零架构改动，学生立刻有感知 | `registerMySQLFunctions:760` 里接着加 `def(...)` |
| A8 | **补 MySQL 语法糖转译**：`DATE_ADD/DATE_SUB(..., INTERVAL n unit)`、`EXTRACT(unit FROM x)`、`TRIM(LEADING/TRAILING/BOTH x FROM y)`、`POSITION(x IN y)`、`CONVERT(x, SIGNED)`、`a DIV b`、`a <=> b`、`INSERT ... SET`、`SELECT ... INTO @var` | 这些都是 MySQL 教材里的高频写法，现在全报 1064 | `translateStatement:449` 加正则重写；`SELECT ... INTO @var` 与 `INSERT ... SET` 在 `runOne` 加分支 |
| A9 | **修 `GROUP_CONCAT` 的 `DISTINCT` + `SEPARATOR` 组合**（§4 P1-5） | 现在 `GROUP_CONCAT(DISTINCT x SEPARATOR '|')` 直接报错 | 剥离 `SEPARATOR` 后再交给聚合，或自建聚合函数 |
| A10 | **`FORMAT()` 语义对齐 MySQL**（千分位 + 四舍五入）（§4 P2-2） | 现在原样返回，属于"假装成功" | `:760` 注册真正的实现 |
| A11 | **`DIFFERENCES` 口径修正 + 与代码做一致性测试**（§5） | 防文档再次漂移 | 加一个 `test-differences.cjs` 断言清单里的每条与实测一致 |
| A12 | **`probe-gaps.cjs` 用例扩容**（§7 附录） | 把"意外 1064 = 0"变成真覆盖 | 把本文 §4 补测的语句加进 `CASES` |

### 3.2 B 档：中等工作量、教学价值高（数天到 1 周）

| # | 功能 | 价值 | 做法 |
|---|---|---|---|
| B1 | **跨库限定名 `db.table`** | 现在 `SELECT COUNT(*) FROM mysql.user` 报 1146，只有先 `USE mysql` 才行；这挡住了"跨库查询/跨库 JOIN"这一整类教学场景 | 现在是"每库一个 SQLite `Database` 对象"（`createDatabaseObject:1052`）。**第一步是技术验证**：确认 sql.js 能否 `ATTACH ':memory:' AS school` 或用 `file:memdb?mode=memory&cache=shared` URI 做单连接多 schema。可行则改造成单连接多 schema，这是打开跨库能力的钥匙 |
| B2 | **外键真实生效** | 现在声明了 FK 但 `PRAGMA foreign_keys = OFF`（`:1055`），插孤儿行照样成功（§4 P0-2）——这是"错答"而非"少做" | 打开 PRAGMA，把 SQLite 的 FK 报错映射到 `1452`/`1451`；同时要回归 seed 数据与 `DROP TABLE` 顺序 |
| B3 | **`EXPLAIN` 输出 MySQL 形态** | 现在直接把 SQLite 的 `id/parent/notused/detail` 原样倒出（`SCAN users`），完全不像 MySQL，而"索引有没有被用上"正是教学重点 | 用 `EXPLAIN QUERY PLAN` 结果 + 自建 catalog 的索引信息，拼出 `id/select_type/table/type/possible_keys/key/rows/Extra` 12 列；再补 `FORMAT=JSON` 与 `EXPLAIN ANALYZE` |
| B4 | **`CREATE TABLE ... AS SELECT` 保留类型** | 现在 `SHOW CREATE TABLE` 出来是 ``\`balance\` num``，MySQL 应推导 `decimal(10,2)` | CTAS 时用源结果集列元数据构造 `metaParsed`（`runOne:1857`） |
| B5 | **`SHOW CREATE TABLE` 统一由 catalog 重生成** | seed 表很完美（有 `ENGINE=InnoDB`/`COMMENT`），但非 seed 建的表回读 `sqlite_master` 原文，措辞是 SQLite 的（§4 P2-6） | 新增 `showCreateFromMeta(meta)`，不走 `raw` |
| B6 | **警告系统** | `SHOW WARNINGS` 现在恒为空集；真 MySQL 里 `INSERT IGNORE` 截断、除零、类型转换都会产生警告 | 在 `Engine` 维护 `this.warnings`，由 `mapError`/DML 分支写入 |
| B7 | **`AUTO_INCREMENT` 起始值/步长 + `TRUNCATE` 重置计数器** | `DIFFERENCES` 已把它列为差异；`TRUNCATE` 后 id 从 1 重新开始是符合直觉的行为 | 元数据层记录计数器，`TRUNCATE` 时重置 |
| B8 | **`/_` 除法语义对齐 MySQL**（§4 P0-3） | `SELECT 7/2` 现在得 `3`，MySQL 得 `3.5000`；这是最容易被学生内化成错误认知的一条 | 在 `translateStatement` 保守重写（如包成 `CAST(a AS REAL)/b`），**必须配一套专门回归用例**，别碰到字符串与日期上下文 |
| B9 | **练习题扩容与分层** | 现在 24 题集中在基础/进阶/挑战三档，窗口函数、CTE、视图、索引、事务没有专门题 | `EXERCISES:2370` 扩到 40–60 题，加"按知识点筛选""错题本""单题重置" |
| B10 | **结果集导出 CSV / JSON** | 学习场景里"把结果带走"很常见 | 客户端生成 Blob 下载，仍然不联网、不上传，卖点不破 |
| B11 | **SQL 脚本导入/导出** | 现在刷新页面数据就回到初始 seed；能存/读 `.sql` 文件会大幅提升可用性 | FileReader 读本地文件 → `engine.run()`；导出用 `SHOW CREATE TABLE` + `INSERT` 拼脚本 |
| B12 | **输入行语法高亮 + 更好的多行编辑** | 交互体验 | `app.js` 输入层 |
| B13 | **引擎放 Web Worker** | 大查询不卡 UI | 需要把 `mysql-core.js` 做成可 postMessage 的形态（它已经不依赖 DOM，改造难度不高） |

### 3.3 C 档：大工程，需产品决策

| # | 功能 | 说明 |
|---|---|---|
| C1 | **简单可更新视图写回基表** | 交接文档 §9 已列为待办。解析视图定义，把 `INSERT/UPDATE/DELETE` 重写到单表基表；这是当前"视图"能力最后一块拼图 |
| C2 | **存储过程/触发器的轻量实现** | 可以用 JS 写一个极简解释器（`BEGIN/END`、`IF`、`LOOP`、`DECLARE`、`CALL`），只在模拟器内执行。教学上是"从 SQL 到编程"的分水岭，工作量最大 |
| C3 | **真正的并发/锁/隔离级别演示** | 需要多 SQLite 连接 + 自研锁管理器，规模大；当前单连接下 `SET TRANSACTION ISOLATION LEVEL` 只能"接受但无效果" |
| C4 | **用户与权限体系** | `CREATE USER`/`GRANT`/`REVOKE` + 真校验（`mysql.user` 表已 seed，可以接上） |
| C5 | **`PREPARE`/`EXECUTE`** | 客户端层做占位替换或走 SQLite 预处理，中等工作量 |
| C6 | **全文检索 `MATCH ... AGAINST`** | SQLite 有 FTS5，可以真做，不是假装 |
| C7 | **分区表** | 可以纯元数据 + `SHOW CREATE TABLE` 展示，实际不分区；要诚实标注 |

---

## 4. 本次新发现的缺陷（现有文档均未登记）

> 以下每条都经实测复现。P0 = 静默给出错误答案，最该修；P1 = 报错或输出形态不对；P2 = 能力缺口或差异未登记。

### P0-1 变参 MySQL 函数注册无效（`GREATEST`/`LEAST` 完全不可用，`CONCAT` 语义反了）

**实测**
```
SELECT GREATEST(1, 5, 3);   → ERROR 1105 (HY000): wrong number of arguments to function GREATEST()
SELECT LEAST(1, 5, 3);      → ERROR 1105 (HY000): wrong number of arguments to function LEAST()
SELECT CONCAT('a', NULL);   → 'a'      -- MySQL 应为 NULL
SELECT CONCAT_WS('-','a',NULL); → 'a'  -- 这条恰好与 MySQL 一致
```

**根因**：`vendor/sql-wasm.js` 的 `create_function` 用 **JS 函数的形参个数**当注册元数：
```js
this.handleError(tb(this.db, g, l.length, 1, 0, n, 0, 0, 0));
//                                  ^^^^^^^^ 注册元数 = fn.length
```
而 `mysql-core.js:760` 的 `def()` 把函数原样传进去。`CONCAT`/`CONCAT_WS`/`FIELD`/`GREATEST`/`LEAST` 都写成 `function () {...}`（变参惯用写法）→ 被注册成 **0 元函数** → 一调用就元数不匹配；若 SQLite 3.49 恰好有同名内建（`concat`/`concat_ws`）就**静默回落到内建语义**，否则直接报错。

**影响**：README 与 `DIFFERENCES` 宣称的 **48 个函数里有 5 个中招**（实测注册数正好 48）：`GREATEST`/`LEAST` 永远报错；`CONCAT` 变成"跳过 NULL"（与 MySQL 相反，也与 `def('CONCAT')` 里的实现意图相反）；`FIELD` 因为 `translateStatement:453` 已把它改写成 `CASE` 才侥幸可用。

**修法**：按元数 1..N 分别注册包装函数（例如 `CONCAT` 注册 1~20 元指向同一实现），或绕过 sql.js 封装直接调 `sqlite3_create_function_v2`（`nArg = -1`）。

### P0-2 外键是摆设：声明了但不生效

**实测**
```
CREATE TABLE parent (id int PRIMARY KEY);
CREATE TABLE child (id int PRIMARY KEY, pid int, FOREIGN KEY (pid) REFERENCES parent(id));
INSERT INTO child VALUES (1, 999);   → Query OK, 1 row affected   -- MySQL 应为 ERROR 1452
SELECT COUNT(*) FROM child;          → 1                          -- MySQL 应为 0
```
**根因**：`createDatabaseObject` 里 `db.run('PRAGMA foreign_keys = OFF;')`（`mysql-core.js:1055`）。
**附带**：`SHOW CREATE TABLE` 会把 FK 原样显示出来，所以用户看到"有外键"却插得进孤儿行，比"不支持外键"更容易误导。`DIFFERENCES` 里也没登记这条。

### P0-3 `/` 是整数除法（`7/2 = 3`）

**实测**：`SELECT 7/2` → `3`；`SELECT 10/4` → `2`；`SELECT 10.0/4` → `2.5`。
**MySQL**：`/` 恒返回 decimal，`7/2` = `3.5000`。
**为什么严重**：这条不在 `DIFFERENCES` 清单里（清单里只提了 `DECIMAL` 不补尾随零、不写 `ORDER BY` 不保证行序），而"整数除整数得整数"是学生最容易照着模拟器内化的错误认知。

### P1-1 `SHOW`/`DESC` 的过滤条件被静默丢弃

```
SHOW DATABASES LIKE 'prod%';          → 返回全部 7 个库（MySQL 只返回 production）
SHOW COLUMNS FROM users LIKE 'id';    → 返回全部 7 列（MySQL 只返回 id）
DESC users username;                  → 返回全部 7 列（MySQL 只返回 username）
```
**根因**：`handleShow:1981` 的 DATABASES 分支没有 LIKE 处理（而同文件 `TABLES:1985`、`VARIABLES:2040`、`FULL TABLES:2085` 都实现了）；`:1997` 的 COLUMNS 分支把 LIKE 与列名过滤都丢掉后直接转 `describeTable`，而 `describeTable:2129` 也不接过滤参数。

### P1-2 `SHOW INDEX` 对"没有索引的表"谎报表不存在

```
CREATE TABLE noidx (a int);
SHOW INDEX FROM noidx;   → ERROR 1146 (42S02): Table 'noidx' doesn't exist   -- MySQL 应返回 Empty set
```
**根因**：`mysql-core.js:2035` —— 索引数组为空时直接返回 1146，把"没索引"误报成"没这张表"。

### P1-3 `TEMPORARY TABLE` 建了却"看不见"

```
CREATE TEMPORARY TABLE t (id int);   → Query OK
INSERT INTO t VALUES (1);            → Query OK
SELECT * FROM t;                     → 有结果        -- 能读能写
DESC t;                              → ERROR 1146   -- 但元数据层不认它
SHOW TABLES;                         → 不含 t       -- 侧栏也看不到
```
**根因**：`tableNames:1085` / `viewNames:1097` / `tableExists:1185` / `objectList:1108` 全部读 `sqlite_master`，而 SQLite 的临时表在 `sqlite_temp_master`。表"半隐身"比直接不支持更让人困惑。

### P1-4 表维护语句的输出形态不对

```
OPTIMIZE TABLE users;  → Empty set (0.00 sec)      -- MySQL 返回 Table/Op/Msg_type/Msg_text
REPAIR TABLE users;    → Empty set (0.00 sec)
FLUSH TABLES;          → Empty set (0.00 sec)
ANALYZE TABLE users;   → Query OK, 0 rows affected -- MySQL 也返回结果集
```
**根因**：`translateStatement:536` 把 `OPTIMIZE/REPAIR/FLUSH` 一律转成 `SELECT 1 WHERE 0`，于是走了 SELECT 分支去打印空结果集。
**现成参照**：同文件的 `CHECK TABLE`（`runOne:1502`）已经做对了，照抄即可。

### P1-5 `GROUP_CONCAT` 的 `DISTINCT` / `ORDER BY` 与 `SEPARATOR` 冲突

```
GROUP_CONCAT(DISTINCT city SEPARATOR '|')        → ERROR 1105: DISTINCT aggregates must have exactly one argument
GROUP_CONCAT(city ORDER BY city SEPARATOR '|')   → 分隔符被丢弃，实际用逗号
GROUP_CONCAT(city SEPARATOR '|')                 → 正确 ✅
```
**根因**：`translateStatement:480` 把 `SEPARATOR x` 变成聚合函数的**第二个实参**；SQLite 的 `DISTINCT` 聚合只允许一个参数，于是报错；而有 `ORDER BY` 时该实参被忽略。

### P2-1 一批 MySQL 合法写法仍报"意外 1064"

（这些**不在** `probe-gaps.cjs` 的用例集里，所以"意外 1064 = 0"的基线没覆盖到）

| 类别 | 语句 | 现状 |
|---|---|---|
| 日期语法 | `DATE_ADD('2026-01-31', INTERVAL 1 MONTH)`、`DATE_SUB(NOW(), INTERVAL 7 DAY)` | 1064 |
| 提取语法 | `EXTRACT(YEAR FROM '2026-05-06')` | 1064 |
| 字符串语法 | `TRIM(LEADING 'x' FROM 'xxabc')`、`POSITION('b' IN 'abc')`（后者报 1146 更离奇） | 1064 / 1146 |
| 运算符 | `7 DIV 2`、`NULL <=> NULL` | 1064 |
| DML 写法 | `INSERT INTO t SET a=1`、`SELECT x INTO @v FROM t` | 1064 |
| 转换 | `CONVERT('123', SIGNED)` | 1305 |
| ALTER | `ENGINE=`、`AUTO_INCREMENT=`、`DROP PRIMARY KEY`、`DROP FOREIGN KEY`、`ADD CONSTRAINT` | 1064（而 `ADD PRIMARY KEY` 有礼貌的 1235，口径不一致） |
| 结构 | `PARTITION BY RANGE ...`、`ADD FULLTEXT INDEX`、`MATCH(x) AGAINST('y')` | 1064 |
| SHOW | `SHOW ENGINE INNODB STATUS`、`SHOW TRIGGERS/EVENTS/PROCEDURE STATUS/OPEN TABLES/BINARY LOGS`、`SHOW COUNT(*) WARNINGS` | 1064 |
| 客户端 | `HELP SELECT` | 1064 |

**建议**：能实现的实现（见 §3.1 A8），实现不了的进 `UNSUPPORTED_STATEMENTS`（A1），能返回空集的返回空集（A2）。**不要留"伪装成 1064"的语句**——这正是项目自己定的规矩。

### P2-2 标量函数缺口与语义不符

- **报 1305 不存在**：`DATEDIFF`、`TIMESTAMPDIFF`、`DAYNAME`、`MONTHNAME`、`WEEK`、`QUARTER`、`DAYOFYEAR`、`STR_TO_DATE`、`SUBSTRING_INDEX`、`ELT`、`FIND_IN_SET`、`ASCII`、`CHAR`、`HEX`、`MD5`、`SHA1`、`CONV`、`MOD`、`POW`、`TRUNCATE`、`STDDEV`、`VARIANCE`、`LAST_INSERT_ID`、`ROW_COUNT`、`FOUND_ROWS`
- **报 1105 参数个数不对**：`LOCATE('b','abc')`（两参版，MySQL 合法）、`MID('abcdef',2)`（两参版）、`NOW(6)`（小数秒）
- **语义不符**：`FORMAT(1234567.891, 2)` 原样返回 `1234567.891`（MySQL 应 `1,234,567.89`）；`SUBSTRING('abcdef',3)` 虽返回正确结果，但实际是 SQLite 内建在兜底，不是本项目注册的那个（同为元数问题）

### P2-3 类型 / 比较语义差异（未登记进 `DIFFERENCES`）

```
SELECT 5 = '5';    → 0    -- MySQL 为 1（字面量之间按文本比；有列参与时又按列亲和性转换，行为不统一）
SELECT 1 + '1';    → 2    ✅ 正常
SELECT id FROM users WHERE id = '1'; → 正常 ✅
SELECT id*2 AS d FROM users HAVING d > 10;  → ERROR 1105 HAVING clause on a non-aggregate query
                                             -- MySQL 允许不带 GROUP BY 的 HAVING
SELECT NULL <=> NULL;  → 1064（见 P2-1）
```

### P2-4 跨库限定名 `db.table` 完全不可用

```
USE production;
SELECT COUNT(*) FROM mysql.user;      → ERROR 1146 Table 'mysql.user' doesn't exist
SELECT COUNT(*) FROM school.students; → ERROR 1146 Table 'school.students' doesn't exist
USE mysql;  SELECT COUNT(*) FROM user; → 3  ✅ 只有"先切库"才行
```
**附带文档问题**：README 第 55 行与交接文档都写"mysql 库中的 user 表可正常查询"，容易让人以为 `mysql.user` 这种限定名可用，实际必须 `USE mysql` 之后写裸表名。

### P2-5 `CREATE TABLE ... AS SELECT` 丢类型元数据

```
CREATE TABLE ctas AS SELECT id, username, balance FROM users WHERE id < 3;
SHOW CREATE TABLE ctas;  → CREATE TABLE `ctas` (`id` int, `username` text, `balance` num)
DESC ctas;               → Type 列显示 int / text / num
```
MySQL 会从源列推导 `int(11)` / `varchar(50)` / `decimal(10,2)`。**修法**：CTAS 时用源结果集的列元数据构造 `metaParsed`（`runOne:1857` 一带）。

### P2-6 `SHOW CREATE TABLE` 对非 seed 表保真度低

seed 表的 `raw` 就是 MySQL 原文，`SHOW CREATE TABLE` 出来带 `ENGINE=InnoDB`、`DEFAULT CHARSET`、`COMMENT`、反引号，效果很好；但用户自己 `CREATE TABLE fk1 (id int PRIMARY KEY, uid int, FOREIGN KEY ...)` 这种（走 SQLite 存原文的路径）出来就是 SQLite 的措辞，没有 `ENGINE`/`CHARSET`。建议统一由 catalog 重新生成。

### P2-7 `EXPLAIN` 直接倒出 SQLite 的查询计划

```
EXPLAIN SELECT * FROM users WHERE city='深圳';
→ 列名是 id / parent / notused / detail，内容是 "SCAN users"
```
MySQL 的 `EXPLAIN` 是 `id/select_type/table/partitions/type/possible_keys/key/key_len/ref/rows/filtered/Extra` 12 列。`EXPLAIN FORMAT=JSON`（`:539` 只剥掉 `FORMAT=`）也被静默忽略。这是教学重点功能，建议做成 MySQL 形态（§3.2 B3）。

### P2-8 `FOR UPDATE` / `LOCK IN SHARE MODE` 被静默删除，无任何提示

`:543` 把这两个子句直接去掉。`DIFFERENCES` 里说明了"没有行级锁"，但**单条语句执行时没有任何提示**，用户会以为锁生效了。建议像 `LOCK TABLES` 那样给一次 `note`。

---

## 5. 文档与代码不一致之处

| # | 位置 | 问题 |
|---|---|---|
| 1 | `DIFFERENCES`（`mysql-core.js:2410`） | 把 `ON DUPLICATE KEY UPDATE` 列进"未支持"，但它**实际可用**（`:459-471`，实测成功写入）；而 `README.md:63` 又把它列进"语法自动转译"。三处口径两两冲突，应以代码为准改文档 |
| 2 | `README.md:61` + `DIFFERENCES` | 宣称"48 个 MySQL 专有函数"，但其中 5 个变参函数注册无效（§4 P0-1） |
| 3 | `README.md:55` | 宣称 `SHOW INDEX/KEYS` 可用；对**没有索引的表**会误报 1146（§4 P1-2） |
| 4 | `README.md:55`、交接文档 §2 | "mysql 库中的 user 表可正常查询"——需先 `USE mysql`；跨库限定名 `mysql.user` 不可用（§4 P2-4） |
| 5 | 交接文档 §8 | "意外语法错 0"是**相对 `probe-gaps.cjs` 那份用例集**的口径；把 `DATE_ADD`/`EXTRACT`/`DIV`/`PARTITION BY` 等加进去就不为 0（§4 P2-1）。建议写明口径并扩容用例 |
| 6 | 交接文档 §1 / §8 指纹 | ✅ **核对通过**：1,102,884 字节 / SHA-256 `16378e5c…d6bddd3`，与文档逐字符一致 |

---

## 6. 建议的推进顺序

| 批次 | 内容 | 改动面 | 风险 |
|---|---|---|---|
| **一、诚实性补漏** | A1–A5、A11、A12（补登记 + 修过滤 + 修空集 + 修输出形态 + 文档口径 + 用例扩容） | `UNSUPPORTED_STATEMENTS`、`handleShow`、`translateStatement` 局部 | 极低，不改引擎语义 |
| **二、正确性修复** | A6–A10、B2、B4、B5、B8（变参函数、GROUP_CONCAT、FORMAT、外键、CTAS 元数据、SHOW CREATE 重生成、除法语义） | `registerMySQLFunctions`、`translateStatement`、catalog | 中，需跑全套测试并回归 seed；除法与外键尤其要配专门用例 |
| **三、能力扩展** | A7、A8 收尾、B3、B6、B7、B9–B11（函数补齐、时间语法糖、EXPLAIN MySQL 化、警告系统、练习题扩容、导出） | 核心层 + 交互层 | 低，纯增量 |
| **四、结构性** | B1（跨库限定名，先做 sql.js `ATTACH` 可行性验证）→ C1（视图可写）→ C6（FTS5 全文检索）→ C2（存储过程）→ C3/C4 | 引擎架构 | 高，需专项设计 |

**每批完成后按交接文档 §5.3 的标准动作走**：
```
改 src/* → python src/build.py → 跑 Node + Playwright 测试确认全绿 → python pack-dist.py
```
（`pack-dist.py` 会把新的指纹写进 `README.txt`，改完 HTML 不重跑会导致用户 `certutil` 校验不一致。）

**最该先做的一件事**：批次一里的 A1 + A2。这两项几乎零风险，却能把"没实现的功能会诚实告知"这个核心卖点从"大部分成立"变成"全面成立"——现在 `SHOW ENGINE INNODB STATUS`、`ALTER TABLE ... ENGINE=`、`PARTITION BY` 这些还在伪装成语法错误。

---

## 7. 附录：建议补进 `probe-gaps.cjs` 的用例

把本次探针里所有"报 1064 / 报错但非预期 / 语义可疑"的语句补进去，让基线变成真覆盖：

```js
// --- 日期与提取语法 ---
"SELECT DATE_ADD('2026-01-31', INTERVAL 1 MONTH);",
"SELECT DATE_SUB(NOW(), INTERVAL 7 DAY);",
"SELECT EXTRACT(YEAR FROM '2026-05-06');",
"SELECT STR_TO_DATE('2026/03/12','%Y/%m/%d');",
"SELECT DATEDIFF('2026-03-01','2026-02-01');",
"SELECT TIMESTAMPDIFF(DAY,'2026-02-01','2026-03-01');",
"SELECT DAYNAME('2026-03-12'), QUARTER('2026-08-01'), WEEK('2026-08-01');",
// --- 字符串与运算符 ---
"SELECT TRIM(LEADING 'x' FROM 'xxabc');",
"SELECT POSITION('b' IN 'abc');",
"SELECT LOCATE('b','abc');",
"SELECT SUBSTRING_INDEX('a.b.c','.',2);",
"SELECT 7 DIV 2;",
"SELECT NULL <=> NULL;",
"SELECT CONVERT('123', SIGNED);",
"SELECT GREATEST(1,5,3), LEAST(1,5,3);",
"SELECT CONCAT('a', NULL);",
"SELECT FORMAT(1234567.891, 2);",
// --- DML 写法 ---
"INSERT INTO users SET username='su', email='su@x.com';",
"SELECT username INTO @un FROM users WHERE id=1;",
// --- ALTER 家族 ---
"ALTER TABLE users ENGINE=MyISAM;",
"ALTER TABLE users AUTO_INCREMENT=1000;",
"ALTER TABLE users DROP PRIMARY KEY;",
"ALTER TABLE users DROP FOREIGN KEY fk_u;",
"ALTER TABLE users ADD CONSTRAINT c FOREIGN KEY (vip_level) REFERENCES users(id);",
// --- 结构与全文检索 ---
"CREATE TABLE part1 (id int, d date) PARTITION BY RANGE (YEAR(d)) (PARTITION p0 VALUES LESS THAN (2020));",
"ALTER TABLE users ADD FULLTEXT INDEX ft (email);",
"SELECT * FROM users WHERE MATCH(email) AGAINST('x');",
// --- SHOW 家族（期望：空结果集而非 1064） ---
"SHOW TRIGGERS;", "SHOW EVENTS;", "SHOW PROCEDURE STATUS;", "SHOW OPEN TABLES;",
"SHOW BINARY LOGS;", "SHOW ENGINE INNODB STATUS;", "SHOW COUNT(*) WARNINGS;",
// --- 过滤条件必须生效（期望：只返回匹配项） ---
"SHOW DATABASES LIKE 'prod%';",
"SHOW COLUMNS FROM users LIKE 'id';",
"DESC users username;",
// --- 表不存在 vs 无索引（期望：后者返回 Empty set） ---
"CREATE TABLE noidx (a int);", "SHOW INDEX FROM noidx;",
// --- 临时表可见性 ---
"CREATE TEMPORARY TABLE t_tmp (id int);", "DESC t_tmp;", "SHOW TABLES LIKE 't_tmp%';",
// --- 表维护输出形态 ---
"OPTIMIZE TABLE users;", "REPAIR TABLE users;", "ANALYZE TABLE users;",
// --- 跨库限定名 ---
"USE production;", "SELECT COUNT(*) FROM mysql.user;", "SELECT COUNT(*) FROM school.students;",
// --- 外键是否生效（期望 1452） ---
"CREATE TABLE fk_p (id int PRIMARY KEY);",
"CREATE TABLE fk_c (id int PRIMARY KEY, pid int, FOREIGN KEY (pid) REFERENCES fk_p(id));",
"INSERT INTO fk_c VALUES (1, 999);"
```

同时建议把 `EXPECTED_UNSUPPORTED` 与新的 `UNSUPPORTED_STATEMENTS` 保持同步，并给"期望报错码"单独建一张表（现在是靠前缀字符串匹配，容易漏）。
