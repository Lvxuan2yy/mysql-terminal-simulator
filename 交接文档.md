# MySQL 终端模拟器（离线单文件版）· 技术交接文档

> 交接对象：接手维护本项目的工程师
> 编写时间：2026-09-21
> 最近更新：2026-09-21（A/B 档功能实施 + 缺陷修复，详见 §13）
> 当前交付件指纹：`mysql-terminal.html` · 1,206,740 字节 · SHA-256 `4f38f83f24483d15dbfff875262e4239741873ddd18ee611d56f12a22441e02f`

---

## 1. 一句话说明

这是一个**在浏览器里运行的 `mysql` 命令行模拟器**：打开一个 HTML 文件，就是一个 `mysql>` 提示符，可以照常敲 SQL，行为、报错码、表格排版都按 MySQL 8.0 的风格来。

底层不是真的 MySQL，而是**编译成 WebAssembly 的 SQLite 3.49.1**，外面套了一层**MySQL 方言兼容层**（本项目自己写的）。整个引擎 + 数据 + 界面全部内联在一个 HTML 文件里，**双击即用、全程不联网、不装任何软件**。

设计目标只有一个：**给学习 SQL 的人一个零安装负担的练习环境**。所以"诚实"比"功能多"更重要——不支持的 MySQL 特性会明确说"不支持 + 为什么"，而不是伪装成能跑。

---

## 2. 能力边界（接手第一件事：搞清楚它不做什么）

| 维度 | 说明 |
|---|---|
| **能做** | 建库建表（含 `CREATE TABLE ... AS SELECT`、表级 `AUTO_INCREMENT=N`）、增删改查、多表 JOIN、派生表、聚合分组、子查询、CTE（含递归）、窗口函数、事务（START TRANSACTION / COMMIT / ROLLBACK / SAVEPOINT）、索引元数据、**视图（VIEW，完整支持）**、**外键真实生效**、系统变量、用户变量与 `SELECT ... INTO @变量`、`EXPLAIN`（12 列 + JSON/TREE/ANALYZE）、`SHOW` 系列、`DESC`、`USE`、99 个 MySQL 函数、**46 道练习题自动判分**、结果导出 CSV/JSON、SQL 脚本导入导出 |
| **不做（刻意）** | 存储过程 / 触发器 / 事件 / `CALL`、`PREPARE`-`EXECUTE` 预处理、`LOAD DATA`、`SELECT ... INTO OUTFILE`、`GROUP BY ... WITH ROLLUP`、多表写（`UPDATE ... JOIN`）、给已有表追加/删除主键、增删外键、改存储引擎、分区表、FULLTEXT/SPATIAL 索引与 `MATCH ... AGAINST`、复制与二进制日志、`XA`、`HANDLER`、真正的并发与锁、`BACKUP DATABASE`（MySQL 社区版本就没有这条 SQL） |
| **架构级限制** | **跨库限定名 `db.table` 不可用** —— 每个逻辑库在底层是各自独立的 SQLite 连接，所以 `SELECT ... FROM mysql.user` 会报 1146，必须先 `USE`。可行性已验证（SQLite 的 `ATTACH` 可用），但改造面很大，见 §13 |
| **语义差异** | 引擎是 SQLite，不是 InnoDB。字符集/排序规则、`AUTO_INCREMENT` 步长、除法显示精度、`EXPLAIN` 的数据来源等与真 MySQL 有出入。**完整清单在程序内「差异」面板**（源码 `mysql-core.js` 的 `DIFFERENCES` 数组），以它为准 |

> 接手时请牢记这条边界：**这个项目"少做一件事"是设计，不是 bug。** 遇到没实现的功能，正确做法是在 `DIFFERENCES` / `UNSUPPORTED_STATEMENTS` 里如实登记并给错误码，而不是硬造一个假的成功。

---

## 3. 目录与文件地图

```
MySQL模拟器/
├── mysql-terminal.html          ← 【成品】单文件交付件（由 build.py 生成，别手改！）
├── 交接文档.md                   ← 本文档
├── README.md                     ← 面向 GitHub 访客的项目说明（另在开源仓库根目录）
├── 功能盘点与可新增清单.md        ← 能力盘点 / 已修缺陷登记 / 可新增功能路线（含证据与行号）
├── .gitignore / .gitattributes   ← 开源仓库的忽略规则与换行符策略（见 §3.1）
├── pack-dist.py                  ← 打包脚本：成品 + README.txt → dist/MySQL终端模拟器-离线单文件版.zip
├── dist/                         ← pack-dist.py 的输出目录（生成物，已被 .gitignore 排除）
│   └── README.txt                       ← 面向普通用户的中文使用说明
├── src/                          ← 【唯一可编辑的源码目录】
│   ├── template.html             ← HTML 骨架 + 5 个注入占位符（134 行）
│   ├── app.css                   ← 全部样式（326 行）
│   ├── app.js                    ← 交互层：终端渲染/键盘/高亮/导出导入/侧栏（1000 行 ≈ 37 KB）
│   ├── mysql-core.js             ← 核心层：方言转译/元数据/排版/错误码/引擎（4110 行 ≈ 216 KB）
│   ├── build.py                  ← 构建：把上面 4 个 + vendor 引擎内联成一个 HTML
│   └── tests/                    ← 全部测试（见 §5）
└── vendor/
    ├── sql-wasm.js               ← sql.js 胶水层（48,788 B）
    └── sql-wasm.wasm             ← SQLite 3.49.1 编译产物（659,806 B，以 base64 内联进成品）
```

> **本仓库已开源**（GitHub 公开仓库）。根目录的 `.gitignore` / `.gitattributes` / `README.md` /
> `THIRD-PARTY-NOTICES.md` 是给公开仓库用的；`dist/` 与测试产生的 `_*_report.txt`、`_shot/`、`_portable/`
> 都已在 `.gitignore` 中排除，不入库。

**铁律：只有 `src/` 下的文件是可编辑的源码。`mysql-terminal.html` 是构建产物，任何手改都会在下一次 `build.py` 时被覆盖。**

---

## 4. 构建流水线

### 4.1 生成成品 HTML

```bash
cd <项目根>/src
python build.py
# → 输出 ../mysql-terminal.html，并打印体积 + 静态检查结果
```

`build.py` 做的事：
1. 读取 `template.html`、`app.css`、`app.js`、`mysql-core.js`、`vendor/sql-wasm.js`；
2. 把 `sql-wasm.wasm` 做 **base64** 编码，塞进 `window.__SQLJS_WASM_B64`；
3. 用 5 个占位符完成替换 —— `/*__CSS__*/`、`/*__SQLJS__*/`、`__WASM__`、`/*__CORE__*/`、`/*__APP__*/`；
4. 对每段 JS 做 `safe_script`（把 `</script>` 转义成 `<\/script>`，防止脚本被提前截断）；
5. **静态检查**：正则扫描 `src=|href=` 的外部 URL 与 `@import`，确认零外部资源。

> 占位符缺一个，`build.py` 会直接退出报错——这是有意的防呆。
> `build.py` 还会 `assert 'wasmBinary' in glue`，确保 sql.js 胶水层支持用内存里的 wasm（而非按路径 fetch 文件）。

### 4.2 打包交付件

```bash
cd <项目根>
python pack-dist.py
# → dist/MySQL终端模拟器-离线单文件版.zip（内含 mysql-terminal.html + README.txt）
```

`pack-dist.py` 会把当前 HTML 的 **大小与 SHA-256 动态写进 README.txt**；`README.txt` 用 **UTF-8 with BOM + CRLF** 写盘（保证 Windows 记事本打开中文不乱码）。

> ⚠️ **改完 HTML 一定要重跑 `pack-dist.py`**，否则 README 里印的指纹会跟实际文件对不上，用户用 `certutil` 校验会提示不一致。

---

## 5. 测试体系与当前基线

测试分两类：**Node 脚本**（无 DOM，直接跑核心层）和 **Playwright 脚本**（真浏览器，跑完整交互）。

### 5.1 Node 侧（`*.cjs`，核心层 / 判定逻辑）

| 文件 | 覆盖内容 | 当前结果 |
|---|---|---|
| `test-core.cjs` | 核心层全量：切分/转译/元数据/排版/错误码 + 全部练习题标准答案逐条执行 | ✅ 全通过 |
| `test-view.cjs` | VIEW 全链路（建/改/删/SHOW/DESC/引用校验/副作用） | ✅ 全通过 |
| `test-exercise.cjs` | 46 题 × 各种等价写法 + 反例 + 串台检查 | ✅ 全通过 |
| **`test-fixes.cjs`** | **修复回归**：把盘点到并修好的每一处缺陷固化成断言（167 条） | ✅ 全通过 |
| `probe-gaps.cjs` | 特性缺口普查：通过 / 诚实 1235 / 预期 1064 / **意外 1064** 四类 | 通过 171 / 1235 24 / 预期 1064 2 / **意外 1064 = 0** |
| `probe-engine.cjs` | 引擎（需 `node` 加载 sql.js） | ✅ |
| `probe-view.cjs` | 视图探针 | ✅ |
| **`e2e-smoke.cjs`** | **无 Playwright 时的浏览器兜底**：注入测试脚本 → 真实键盘事件驱动终端 → 抓 DOM 校验（26 项） | ✅ 26/26 |

跑法（Node 用 managed 版本）：

```bash
NODE="<你的 node 可执行文件>"
cd <项目根>/src/tests
NODE_PATH=../.. "$NODE" test-core.cjs      # 报告写到 _test_report.txt
"$NODE" test-view.cjs
"$NODE" test-exercise.cjs
"$NODE" test-fixes.cjs                     # 修复回归（167 条断言）
"$NODE" probe-gaps.cjs                     # 缺口普查（断言"意外 1064 = 0"）

# 没装 Playwright 时的浏览器兜底（零依赖，用本机 Edge 无头模式）
"$NODE" e2e-smoke.cjs                      # 报告写到 _e2e_report.txt
```

### 5.2 浏览器侧（`*.py`，Playwright）

| 文件 | 覆盖内容 | 当前结果 |
|---|---|---|
| `test-browser.py` | 端到端交互（终端/侧栏/练习打勾） | ✅ |
| `test-terminal.py` | 终端行为 26 项（输入行固定、clear 不毁历史、重连全重置、边界） | ✅ 全部通过 |
| `test-exercise-ui.py` | 复现"只查两列没打勾"截图场景 | ✅ |
| `test-portable.py` | 可移植性 5 项（ASCII/中文路径、断网、禁 localStorage、老内核） | ✅ 5/5 |
| `test-crossbrowser.py` | chromium / firefox / webkit × 中英路径 | ✅ 6/6 组，各 17/17 |
| `test-extreme.py` | 文件截断/损坏兜底（截 10%、只传一半、头部改 1 字节） | ✅ 均有友好提示，不白屏 |
| `audit-sideeffects.py` | 副作用审计 | ✅ |
| `shot-view.py` | 截图工具 | — |

跑法：

```bash
cd <项目根>/src/tests
python test-terminal.py        # 报告写到 _terminal_report.txt
python test-crossbrowser.py
python test-portable.py
python test-extreme.py
```

> 所有脚本都把报告写成 `_*_report.txt`，**不刷屏**。改完代码先看这几个报告文件确认全绿。

### 5.3 改动后的标准动作（务必遵守）

```
改 src/*.js|css|html
   → python src/build.py            （重建成品）
   → 跑 Node 测试 + Playwright 测试  （确认全绿）
   → node src/tests/e2e-smoke.cjs   （没装 Playwright 时的浏览器兜底）
   → python pack-dist.py            （重新打包，刷新 README 指纹）
```

---

## 6. 架构说明

### 6.1 四层结构

```
┌─────────────────────────────────────────────┐
│ template.html   骨架 + 启动自检守卫           │
├─────────────────────────────────────────────┤
│ app.js          交互层（依赖 DOM、依赖 Core）  │
│                 终端渲染 / 键盘 / 历史 / 补全   │
├─────────────────────────────────────────────┤
│ mysql-core.js   核心层（无 DOM，Node 可跑）    │
│                 方言转译 / 元数据 / 排版 / 错误码│
├─────────────────────────────────────────────┤
│ vendor/sql.js   SQLite 3.49.1 (WASM)         │
└─────────────────────────────────────────────┘
```

核心层**刻意不依赖 DOM**，才能在 Node 里直接 `require` 做回归测试——这是整个测试体系能跑起来的前提，接手时不要破坏这条边界。

### 6.2 `mysql-core.js` 章节地图（0–13）

| 节 | 内容 | 关键函数/对象 |
|---|---|---|
| 0 | 常量 | `SERVER_VERSION`(`8.0.36-sim`)、`SYSTEM_DATABASES` |
| 1 | 文本宽度与对齐 | `cpWidth` / `dispWidth`（**正确处理 CJK 全角宽度**，表格对齐靠它） |
| 2 | 语句切分 | `splitStatements`（引号/反引号/`--`/`#`/块注释/`\G` 纵向输出） |
| 3 | 建表语句解析（元数据 catalog） | `parseCreateTable` —— 自己解析 DDL 建列目录（含表级 `AUTO_INCREMENT=N`），因为 SQLite 拿不到 MySQL 式元数据 |
| 4 | **MySQL → SQLite 方言转译** | `translateStatement`（表选项尾巴、KEY/INDEX、列修饰、AUTO_INCREMENT、ENUM/SET/JSON→TEXT、死逗号清理、**运算符语义（`/`、`DIV`、`<=>`）**、**MySQL 函数语法糖**） |
| 4b | 转译用的文本工具 | `mapOutsideQuotesAndComments` / `replaceOutsideAll`（**认引号与注释**，替换必须走它们）、`rewriteGroupConcat`、`rewriteField`、`rewriteTrim`/`rewritePosition`/`rewriteConvert`/`rewriteExtract`/`rewriteDateFunctions`/`rewriteIsNull`/`rewriteInsertFunc`、`rewriteFunctionCalls` |
| 5 | 结果集排版 | `formatTable` / `formatVertical` / `rowsTail`（`N rows in set (0.00 sec)`） |
| 6 | 错误映射 | `mapError` —— 把 SQLite 报错翻成 MySQL 错误码（含外键 1452/1451 按语句类型区分） |
| 7 | MySQL 函数注册 | `registerMySQLFunctions`（99 个函数；`def` / `defVariadic` / `def0or1`，**注意 sql.js 用 fn.length 当 nArg**） |
| 7b | 函数实现用的纯 JS 工具 | `parseDateTime` / `addMonthsClamped` / `timestampDiffImpl` / `strToDateImpl`、`md5Hex` / `sha1Hex` / `sha256Hex`（纯 JS 哈希，带标准测试向量回归） |
| 8 | 虚拟结果集构造 | 变量查询、`SHOW` 结果的构造 |
| 8.1b | **EXPLAIN** | `parsePlanLine` / `Engine.explainStatement` —— 把底层查询计划翻成 MySQL 的 12 列 / JSON / TREE / ANALYZE |
| 8.1c | CTAS 列类型推导 | `Engine.ctasColumnTypes` / `Engine.buildCtasMeta` |
| 8.1d | AUTO_INCREMENT 起始值 | `Engine.applyAutoIncrement`（把自增列的显式值补进 INSERT） |
| 8.2a | SHOW 系列共用工具 | `splitDbTable` / `likeRegex` / `filterShowRows` / `describeResult` / `showCreateTable` |
| 9 | **引擎** | `Engine` 类（见下） |
| 10 | 示例数据 | `SEED` / `seedAll`（内置几个库和表，供练习） |
| 11 | 练习题 | `EXERCISES`（46 题，四档：基础/进阶/挑战/实战） |
| 12 | 与真实 MySQL 的差异 | `DIFFERENCES`（**诚实清单**，程序「差异」面板直接渲染它） |
| 13 | 导出 | 统一 `return {...}`，同时支持浏览器全局 `MySQLCore` 与 Node `module.exports` |

**`Engine` 类主要方法**（第 9 节）：

- 生命周期：`init`（建系统库）、`run`（跑一段输入）、`runOne`（跑单条语句，**命令分发中枢**）
- 元数据：`databaseNames` / `tableNames` / `viewNames` / `viewExists` / `objectList` / `meta` / `tableExists` / `describeTable` / `viewDef` / `buildViewMeta`
- 变量：`systemVarMap` / `readVar` / `substituteVars` / `handleSet`
- 命令：`handleShow`（`SHOW` 系列）、`banner`
- 输出：`resultBlock` / `errBlock` / `statusText`
- 其他：`findDuplicateValue` / `syncCatalogAfter` / `resolveDb`

### 6.3 `app.js` 分区

| 区块 | 内容 |
|---|---|
| 工具 | `$` / `b64ToU8`（wasm base64 解码）/ `toast` |
| 终端输出 | `line` / `renderTableList` 等 |
| 输入区 | 隐藏 `<input>` 承接键盘 + 中文输入法，视觉上是自绘的 `mysql>` 提示符 |
| **语法高亮** | `appendHighlighted` —— 把输入文本按词法切成 span（关键字/字符串/数字/注释），再把光标 span 精确插进去；**只影响显示**，真正的输入仍在隐藏 `<input>` 里 |
| 执行 | 拿 `buffer` → `engine.run()` → 渲染 |
| 历史与补全 | ↑↓ 翻历史、Tab 补关键字/表名、Ctrl+C 放弃 |
| 练习题完成度 | `markDone` → 调 `Core.matchExercises` 判分 → 写 localStorage |
| **工具面板** | `exportCSV` / `exportJSON` / `exportDump`（读 `engine.lastResultSet` 与 `engine.dumpDatabase()`）/ `importSQL`（FileReader → `Core.splitStatements` 逐条执行）。全部本地 Blob/FileReader，不联网 |
| 侧栏 | 五个标签：数据库 / 练习 / **工具** / 差异 / 帮助 |
| 事件绑定 / 启动 | 按钮、快捷键、引擎初始化 |

### 6.4 关键机制 A：终端回滚（为什么 `clear` 不毁历史）

**需求**：输入行永远固定在底部；`clear` / `Ctrl+L` 只清"当前一屏"，往上滚还能回看旧命令；只有「重新连接」才彻底清空。

**实现**（`app.js` 顶部注释与 `clearScreen` 一带）：

- `clearScreen()` **不再** `innerHTML = ''`，而是插入一个零高度的 `.clr-anchor` 锚点，再插入一个撑满"一屏剩余高度"的 `.clr-spacer` 空白块。
- 旧内容全部留在 DOM 里 → 浏览器向上滚就是回滚缓冲，天然可回看。
- 新输出用 `insertBefore(div, activeSpacer)` 插在空白块**之前** → 观感是"新内容从屏幕顶部往下填、空白留在下方"，与真实终端一致。
- 空白高度用 `getBoundingClientRect()` 计算，**不能用 `offsetTop`**（`.scroll` 没有 `position:relative`，`offsetTop` 会算错）。
- `trimScrollback()` 限制回滚缓冲 **5000 行**，但**永不删到锚点之前**。
- `reconnect()` 是唯一调 `resetScreen()`（真 `innerHTML=''`）的地方，同时清空 `history` / `hIdx` / `draft`。
- ⚠️ **已知设计决策**：「重新连接」**不清空练习进度**（进度在 `localStorage`，key = `mysqlsim.done.v2`）。刷新页面、重连都还在。这是刻意的，README 里也写明了。如果产品上想改成"重连=全清"，只需在 `reconnect()` 里加一句清 `doneSet` + localStorage。

### 6.5 关键机制 B：练习题判定（为什么"只查两列"曾不打勾）

**背景 bug**：用户写了 `select username,email from users;`，第 6 题（"只查询用户名和邮箱两列"）没打勾。

**根因**：旧实现 `norm()` 只做 `toLowerCase()` + 空白折叠，然后**全字符串相等**比较。`select username,email` 与参考答案 `select username, email`（逗号后有空格）不相等 → 判定失败。

**修复**：把判分逻辑从交互层搬到核心层，新增两级规范化 + 匹配函数：

```js
canonicalSQL(sql)         // 一级：去大小写、去多余空格、去逗号/括号两侧空格、
                          //       去反引号、去结尾 ; 和 \G
canonicalSQLRelaxed(sql)  // 二级：在一级基础上再剥离单字母表别名
                          //       (SELECT u.username ... FROM users u ≡ SELECT username ... FROM users)
matchExercises(text, list)// 按「语句」逐条比对（不做子串匹配！），返回命中的题目下标数组
```

**三个必须记住的坑**（都踩过、都已在代码里修好）：

1. **反引号必须在状态机之前剥掉**。`replaceOutsideQuotes` 会把 `` ` `` 当成引号起点，导致反引号永远清不掉。正确顺序：先 `s.replace(/`/g,'')`，再进状态机。
2. **大小写转换必须"跳过引号内"**。直接 `s.toLowerCase()` 会把字符串字面量也小写，造成 `'ABC' ≡ 'abc'` 的误判。必须用 `replaceOutsideQuotes(s, /[A-Z]+/g, m => m.toLowerCase())`。
3. **比较单位是"整条语句"，不是子串**。否则 `SELECT username FROM users` 会被判成含有 `username` 而误命中"只查两列"这题。

判定标准必须是"同一条语句的不同写法"，只忽略：大小写、多余空格、逗号/括号两侧空格、反引号、单字母别名。**列数、条件、表名必须真的对得上。**

回归覆盖：`test-exercise.cjs`（152 种写法变体 + 11 个反例 + 串台检查）、`test-exercise-ui.py`（浏览器复现原截图）。

### 6.6 关键机制 C：视图（VIEW）支持

SQLite 引擎层**本来就能** `CREATE VIEW`，但要让它看起来像 MySQL，得把元数据层全部接上：

- 数据库对象新增 `views: {}`；新增 `viewNames` / `viewExists` / `viewDef` / `buildViewMeta` / `objectList`。
- `Engine.runOne` 增加视图分支：`CREATE [OR REPLACE] VIEW`、`ALTER VIEW`、`DROP VIEW`、以及 `DROP TABLE` 命中视图（→ 1347）。
  - `IF NOT EXISTS` 与 `OR REPLACE` **互斥**（同时出现 → 1064）。
  - 解析时按顶层 `AS` 关键字切开（`topLevelKeywordIndex(trimmed,'AS')`），再从头剥 `IF NOT EXISTS`。
- **关键点：建视图后要主动校验引用**。SQLite 对视图引用的表/列是**惰性校验**（真 MySQL 是建视图时就校验），所以必须建完立刻探一次 `SELECT * FROM v LIMIT 0`：引用不存在 → 报 1146 / 1054，并把半成品视图 DROP 掉，不给用户留一个用不了的视图。
- `handleShow` 覆盖：`SHOW TABLES`（含视图）、`SHOW FULL TABLES`（`Table_type` 列标 `BASE TABLE`/`VIEW`）、`SHOW CREATE VIEW`、`SHOW CREATE TABLE` 作用于视图、`SHOW TABLE STATUS`、`SHOW INDEX FROM 视图`（→ 1347）。
- `mapError` 增加：改视图走 DML 的报错 → 1288 `... is not updatable`；`DROP TABLE` 想删视图 → 1347。
- UI：左侧表列表用 `◇` 标记视图，紫色，显示"视图 · N 列"（`.tblrow.isview .glyph`、`.tblrow .cols.vw`）。

**明确不支持的**：真实 MySQL 允许对"简单可更新视图"直接 `INSERT/UPDATE/DELETE` 并写回基表，本模拟器不支持，报 `ERROR 1288`。这条也写在 README 里。

### 6.7 关键机制 D：离线内联 & 启动自检

- **离线**：`sql-wasm.wasm` 以 base64 塞进 `window.__SQLJS_WASM_B64`，启动时 `initSqlJs({ wasmBinary: b64ToU8(...) })` —— 全程不发起任何网络请求（`test-portable.py` 有"断网运行 + 外部请求 0 条"的断言）。
- **启动自检守卫**：`template.html` 开头有一段刻意做得极小的脚本（放在文件最前面，保证只要文件开头完好就能生效）。它起一个 **15 秒定时器**，若到点 `window.__BOOT_SETTLED` 仍为 false，就把启动遮罩换成友好错误文案（并区分"wasm 数据读到了但没跑完" vs "连 wasm 数据都没读到"），提示文件可能被截断、或浏览器内核太旧。
  - 正常启动流程无论成功失败都会置 `__BOOT_SETTLED = true`。
  - 这段是为了防"文件在传输/下载过程中被截断，用户永远卡在初始化界面却毫无线索"。

### 6.8 明确不支持语句的登记方式

`mysql-core.js` 里有 `UNSUPPORTED_STATEMENTS` 表，每项 4 个字段，最后一个 `code` 指定错误码：

- 需要"诚实的 1064"的（如 `BACKUP` / `RESTORE` / `MYSQLDUMP`）→ `code: 1064` + 附解释；
- 需要"这个功能本模拟器没做"的（如 `PREPARE`、`LOAD DATA`）→ 默认 1235。

---

## 7. 关键设计决策与已知限制（沿用即可，别当 bug 修）

1. **底层是 SQLite 不是 MySQL** —— 一切行为差异以 `DIFFERENCES` 清单为准，程序内可查。
2. **`BACKUP DATABASE` 不做** —— MySQL **社区版本身就没有这条 SQL**，它是企业版（MySQL Enterprise Backup）的商业组件；社区版备份靠命令行 `mysqldump`，不是在 `mysql` 客户端里敲的。模拟器如实报 1064 + 说明，**没有凭空造一条 MySQL 不存在的命令**。
3. **视图不可写**（`INSERT/UPDATE/DELETE` 视图 → 1288）。
4. **重连不清练习进度**（localStorage 持久）。
5. **回滚缓冲上限 5000 行**，超出丢最旧的行。
6. **多表写不支持**（`UPDATE/DELETE ... JOIN` → 1235，建议改写成 `WHERE EXISTS (SELECT 1 ...)`）。
7. **`GROUP BY ... WITH ROLLUP` 不支持**（SQLite 不会自动生成小计行）。
8. **给已有表追加主键不支持**（SQLite 需重建整表）。

---

## 8. 当前状态与验证基线

| 项目 | 状态 |
|---|---|
| 成品 HTML | ✅ 1,206,740 字节，SHA-256 `4f38f83f…` |
| 核心层测试 | ✅ 全通过，无未捕获异常 |
| 视图测试 | ✅ 全通过 |
| 练习题判定 | ✅ 46/46 全通过（含变体 + 反例 + 串台） |
| 修复回归 | ✅ `test-fixes.cjs` 167 条断言全通过 |
| 终端行为 | ✅ 26 项全通过（需 Playwright） |
| 可移植性 | ✅ 5/5（需 Playwright） |
| 跨浏览器 | ✅ chromium / firefox / webkit 各 17/17（需 Playwright） |
| 极端情况 | ✅ 截断/损坏均有友好提示，不白屏（需 Playwright） |
| 特性缺口普查 | ✅ 通过 171 / 诚实 1235 共 24 / 预期 1064 共 2 / **意外语法错 0** |
| 浏览器兜底（Edge 无头） | ✅ 26/26 |

---

## 9. 待办与未决事项

以下均为 **MySQL 模拟器本体** 的迭代项，都不是缺陷，按需取舍：

- [ ] **跨库限定名 `db.table`**：可行性已验证（sql.js 支持 `ATTACH DATABASE ':memory:' AS name`，跨 schema 查询可用），但需要把"每库一个 SQLite 连接"改成"单连接多 schema + 把未限定表名按当前库改写"，改造面覆盖 `Engine` 全部数据访问路径，风险高。建议作为独立专项，先在分支上做，并保证 `test-core.cjs` / `test-fixes.cjs` 全绿再合。
- [ ] **视图可写**（简单可更新视图写回基表）——工作量较大，当前明确不做（报 1288）。
- [ ] **Web Worker** 里跑引擎，避免大查询卡 UI：`mysql-core.js` 本来就不依赖 DOM，改造可行性高；但单文件离线版要把 wasm + 核心层塞进 Blob Worker，需重新验证"零外部请求"这条不变量，风险中等。
- [ ] 是否把「重新连接」改成**同时清空**练习进度（当前刻意为不一致地保留了）——需产品确认。
- [ ] 更多练习题 / 更细的难度分层（现在是 4 档 46 题）。
- [ ] 若要发布为在线版，需另配托管（当前定位是「离线单文件」，本身不需要）。

---

## 10. 踩过的坑（接手避雷）

1. **`python-docx` 不适用本项目**，但同类坑：读某些带坏 rels 的 office 文件会崩 —— 本项目不涉及，略过。
2. **Bash 工具下 `/tmp` 不可用于 Python** —— 脚本与临时输出一律写到 Windows 路径下的临时目录（例如 `D:\_tmp\`）。
3. **`Remove-Item` 被 safe-delete 拦截**，`trash` 对**含中文/非 ASCII 的路径**会 aborted。删文件用 `[System.IO.File]::Delete($full)`，删目录用 `[System.IO.Directory]::Delete($path, $true)`。
4. **PowerShell 工具不回显 stdout** —— 需要看输出时写文件再用 Read 读（本项目 Node/Python 脚本都自带写报告文件，天然规避）。
5. **改判定逻辑后，务必同时跑 `test-exercise.cjs` 和 `test-exercise-ui.py`** —— 前者查逻辑，后者查真实浏览器里的打勾行为，两者不可互相替代。
6. **改终端相关代码后，必须跑 `test-terminal.py`** —— 回滚/清屏/重连这三件事很容易回归时被悄悄改坏。
7. ⚠️ **sql.js 的 `create_function` 用 JS 函数的 `fn.length` 当注册的 nArg**（见 `vendor/sql-wasm.js` 里的 `tb(this.db, g, l.length, ...)`）。所以：
   - 写成 `function () {...}` 的**变参**函数会被注册成 **0 元函数**，一调用就报 `wrong number of arguments`；若 SQLite 恰好有同名内建（`concat`/`concat_ws`）就更糟——**静默回落到内建语义**。
   - 想支持 n 个参数，就得用 `defVariadic`（`mysql-core.js` 里的 `ARITY_FACTORY`）按 1..16 逐个元数注册；参数个数固定的函数要**每个元数都写一份**（例如 `WEEK(d)` 与 `WEEK(d,mode)`、`SUBSTRING(s,p)` 与 `SUBSTRING(s,p,n)`）。
   - 新增函数后跑一下 `db.__mysqlFnErrors`（`test-fixes.cjs` 里有断言），确认注册没失败。
8. ⚠️ **`translateStatement` 里做文本替换时必须用 `mapOutsideQuotesAndComments` / `replaceOutsideAll`**，不要直接 `String.replace`：注释里的 `/` 会被除法改写规则误伤，字符串字面量里的 `'` 会被引号状态机吃掉。
9. ⚠️ **PowerShell 的 `Get-Content` 默认按系统代码页解码**，读 UTF-8 中文文件会读出乱码（看起来像文件坏了，其实是显示的锅）。要读 UTF-8 内容请用 `Get-Content -Encoding utf8`，或者干脆用 Node 读写（本项目脚本都走 Node/Python 的 `utf8`）。
10. ⚠️ **`python build.py` / `pack-dist.py` 在 GBK 控制台下会因为打印 ✅ emoji 报 `UnicodeEncodeError`**（构建本身其实是成功的）。跑之前设 `$env:PYTHONIOENCODING='utf-8'` 与 `$env:PYTHONUTF8='1'`。
11. ⚠️ **无头浏览器验证的坑**：`msedge --headless=new --dump-dom` 配合 `--virtual-time-budget` 时，页面里的**高频 `setInterval` 会瞬间烧完虚拟时间预算**，页面还没跑完就被 dump 了（表现为"抓不到测试结果"）。`e2e-smoke.cjs` / `e2e-inject.js` 因此改用 `MutationObserver` 等启动完成；另外 headless Edge 常常不自己退出，脚本用 `taskkill /PID <pid> /T /F` 收进程树。
12. **练习题的参考答案必须"自包含"**：`test-core.cjs` 会在**每题之前 `eng.init()` 重置引擎**，所以依赖上一题副作用（比如上一题建的视图）的题目会失败。新加练习题后一定要跑 `test-core.cjs`。

---

## 11. 快速命令速查

```bash
# 构建成品
cd <项目根>/src && python build.py

# 打包交付件（会刷新 README 里的指纹）
cd <项目根> && python pack-dist.py

# Node 核心层测试
NODE="<你的 node 可执行文件>"
cd <项目根>/src/tests
NODE_PATH=../.. "$NODE" test-core.cjs
"$NODE" test-view.cjs
"$NODE" test-exercise.cjs
"$NODE" test-fixes.cjs
"$NODE" probe-gaps.cjs
"$NODE" e2e-smoke.cjs        # 无 Playwright 时的浏览器兜底

# Playwright 浏览器测试
python test-browser.py
python test-terminal.py
python test-exercise-ui.py
python test-portable.py
python test-crossbrowser.py
python test-extreme.py

# 校验成品指纹（Windows 命令行）
certutil -hashfile mysql-terminal.html SHA256
```

---

## 12. 附录：环境与版本

| 项 | 值 |
|---|---|
| 引擎 | sql.js / SQLite **3.49.1**（WASM，659,806 B） |
| 模拟的 MySQL 版本号 | `8.0.36-sim`（`SERVER_VERSION`；`VERSION()` 返回 `8.0.36-mysql-sim`） |
| 源码规模 | `mysql-core.js` 4110 行 / `app.js` 1000 行 / `app.css` 326 行 / `template.html` 134 行 |
| 注册的 MySQL 函数 | 99 个（注册条数 105，变参函数按多个元数重复注册） |
| 练习题 | 46 题，4 档 |
| 管理 Node（本机示例） | `<你的 node 路径>`（Node 18+ 均可，用系统自带的也行） |
| 管理 Python venv（本机示例） | `<你的 python 路径>`（需先 `pip install playwright`） |
| 浏览器要求 | Chrome 57+ / Edge 16+ / Firefox 52+ / Safari 11+（双核浏览器需切「极速模式」） |

---

## 13. 本轮迭代记录（2026-09-21）

这一轮把《功能盘点与可新增清单.md》里的 A 档与 B 档全部实施，并修掉了盘点中定位到的缺陷。改动集中在 `src/mysql-core.js` 与 `src/app.js`，**没有改动引擎（vendor）与单文件离线架构**。

### 13.1 修掉的缺陷（每条都有断言守着）

| # | 缺陷 | 根因 | 回归断言 |
|---|---|---|---|
| 1 | `GREATEST`/`LEAST` 完全不可用；`CONCAT` 遇 NULL 不返回 NULL | sql.js 用 `fn.length` 当 nArg，变参函数被注册成 0 元（详见 §10.7） | `test-fixes.cjs` A6 组 |
| 2 | 外键是摆设（孤儿行照样插得进去） | `PRAGMA foreign_keys = OFF` | P0-2 组 |
| 3 | `SELECT 7/2` 得 3（整数除法） | SQLite 对两个整数做整除 | P0-3 组 |
| 4 | `SHOW DATABASES LIKE` / `SHOW COLUMNS LIKE` / `DESC 表 列` 的过滤条件被静默忽略 | `handleShow` 各分支没接过滤参数 | A3 组 |
| 5 | `SHOW INDEX` 对"没有索引的表"谎报 1146 | 索引数组为空时直接报"表不存在" | A4 组 |
| 6 | `TEMPORARY TABLE` 能读写却看不见 | 只查了 `sqlite_master`，临时表在 `sqlite_temp_master` | P1-3 组 |
| 7 | `OPTIMIZE/REPAIR TABLE` 输出 `Empty set` | 被转译成 `SELECT 1 WHERE 0` | A5 组 |
| 8 | `GROUP_CONCAT(DISTINCT x SEPARATOR s)` 报错、带 `ORDER BY` 时分隔符被丢弃 | `SEPARATOR` 被塞成第二个实参 | A9 组 |
| 9 | 一批 MySQL 语法报"假 1064" | 没登记进 `UNSUPPORTED_STATEMENTS` | A1/A2 组 + `probe-gaps.cjs` |
| 10 | `DROP TEMPORARY TABLE` 报 1064 | `DROP TABLE` 正则不认 `TEMPORARY` | `probe-gaps.cjs` |
| 11 | `SHOW CREATE TABLE` 对用户建的表回显 SQLite 措辞 | 直接回读 `sqlite_master` 原文 | P2-6 组 |
| 12 | `CREATE TABLE ... AS SELECT` 丢类型（显示 `int/text/num`） | 没建 MySQL 风格目录 | P2-5 组 |
| 13 | `EXPLAIN` 直接倒出 SQLite 的 `id/parent/notused/detail` | 未做形态转换 | B3 组 |
| 14 | `FORMAT()` 原样返回（假装成功） | 落到了 SQLite 的 printf 上 | A6 组 |
| 15 | `CONCAT`/`SHOW` 家族之外，`LOCATE(x,y)`、`MID(x,p)`、`NOW(6)` 等元数不对 | 同上 | A6 组 |

### 13.2 新增能力

- **语法糖**：`a DIV b`、`a <=> b`、`CONVERT(x, type)`、`TRIM(LEADING/TRAILING/BOTH x FROM y)`、`POSITION(x IN y)`、`ISNULL(x)`、`EXTRACT(unit FROM x)`、`DATE_ADD/DATE_SUB/ADDDATE/SUBDATE(..., INTERVAL n unit)`、`TIMESTAMPDIFF/TIMESTAMPADD`、`INSERT()` 字符串函数、`INSERT ... SET`、`SELECT ... INTO @变量`、`HELP <topic>`。
  - 日期运算是**用 JS 实现的 MySQL 语义**（`DATE_ADD('2026-01-31', INTERVAL 1 MONTH)` 得 `2026-02-28`），没有走 SQLite 的 `datetime(x,'+1 month')`（那会给 3 月 3 日）。
- **函数补齐**（+51 个）：日期时间（`DATEDIFF`/`TIMESTAMPDIFF`/`DAYNAME`/`MONTHNAME`/`WEEK`/`QUARTER`/`DAYOFYEAR`/`STR_TO_DATE`/`SEC_TO_TIME`…）、字符串（`SUBSTRING_INDEX`/`ELT`/`FIND_IN_SET`/`ASCII`/`CHAR`/`HEX`/`UNHEX`/`CONV`/`SPACE`…）、数值（`MOD`/`POW`/`TRUNCATE`/`SIGN`…）、哈希（`MD5`/`SHA1`/`SHA2`，纯 JS 实现并带标准测试向量）、统计聚合（`STDDEV`/`VARIANCE` 系列，Welford 算法）、会话信息（`LAST_INSERT_ID`/`ROW_COUNT`/`FOUND_ROWS`/`INET_ATON`…）。
- **`EXPLAIN` 四形态**：传统 12 列 / `FORMAT=JSON` / `FORMAT=TREE` / `ANALYZE`（会真跑一次给出真实行数）。
- **警告系统**：`SHOW WARNINGS` / `SHOW COUNT(*) WARNINGS` 有真实内容（除零 1365、`INSERT IGNORE` 跳过的 1062）。
- **AUTO_INCREMENT 起始值**：`CREATE TABLE ... AUTO_INCREMENT=N` 与 `ALTER TABLE ... AUTO_INCREMENT=N` 生效（对 `INSERT ... VALUES` 形式），`TRUNCATE` 重置回 1。
- **`SHOW` 家族**：与"没有建模的对象"相关的语句返回规范的空结果集；`SHOW ENGINE INNODB STATUS` 明确说明未实现。
- **练习题** 24 → 46 题，新增「实战」档（视图、索引、EXPLAIN、保存点、CTE、窗口函数、JSON、日期运算）。
- **界面**：新增「工具」面板（导出结果 CSV/JSON、导出当前库为 `.sql`、导入本地 `.sql`）、输入行 SQL 语法高亮、练习档位与题数动态显示。

### 13.3 明确暂缓的两项（含取舍理由）

- **跨库限定名 `db.table`**：可行性已实测确认（`ATTACH DATABASE ':memory:' AS name` 可用，`SELECT a FROM m2.t` 能查到数据），但要把 `Engine` 从"每库一个 SQLite 连接"改成"单连接多 schema"，并给所有未限定表名补上当前库的 schema 前缀 —— 触及引擎所有数据访问路径，回归风险高。**本轮只做了两件低风险的事**：支持限定到当前库的 `db.tbl` 写法（`DESC production.users`、`SHOW CREATE TABLE production.users` 等），以及把限制如实写进 `DIFFERENCES` 与 README。
- **Web Worker**：`mysql-core.js` 不依赖 DOM，改造本身可行；但单文件版要把 wasm + 核心层塞进 Blob Worker，需要重新验证"全程零外部请求"这条不变量（`test-portable.py` 的断言语境会变）。收益（大查询不卡 UI）与风险不成比例，**建议等有真实卡顿反馈再做**。

---

**交接验收建议**：接手后依次执行 §11 的「构建 → 打包 → 全部测试」三条链路，确认输出全绿、且 `pack-dist.py` 打印的 SHA-256 与 §1 一致，即视为环境就绪。
