# MySQL 终端模拟器（离线单文件版）· 技术交接文档

> 交接对象：接手维护本项目的工程师
> 编写时间：2026-09-21
> 当前交付件指纹：`mysql-terminal.html` · 1,102,884 字节 · SHA-256 `16378e5c33d22be215fe00bfb3c7725e8c21a1fa52d28e6d56ee4c672d6bddd3`

---

## 1. 一句话说明

这是一个**在浏览器里运行的 `mysql` 命令行模拟器**：打开一个 HTML 文件，就是一个 `mysql>` 提示符，可以照常敲 SQL，行为、报错码、表格排版都按 MySQL 8.0 的风格来。

底层不是真的 MySQL，而是**编译成 WebAssembly 的 SQLite 3.49.1**，外面套了一层**MySQL 方言兼容层**（本项目自己写的）。整个引擎 + 数据 + 界面全部内联在一个 HTML 文件里，**双击即用、全程不联网、不装任何软件**。

设计目标只有一个：**给学习 SQL 的人一个零安装负担的练习环境**。所以"诚实"比"功能多"更重要——不支持的 MySQL 特性会明确说"不支持 + 为什么"，而不是伪装成能跑。

---

## 2. 能力边界（接手第一件事：搞清楚它不做什么）

| 维度 | 说明 |
|---|---|
| **能做** | 建库建表、增删改查、多表 JOIN、聚合分组、子查询、事务（START TRANSACTION / COMMIT / ROLLBACK / SAVEPOINT）、索引、**视图（VIEW，完整支持）**、系统变量、`EXPLAIN`、`SHOW` 系列、`DESC`、`USE`、用户变量 `@x`、24 道练习题自动判分 |
| **不做（刻意）** | 存储过程 / 触发器 / 事件、`PREPARE`-`EXECUTE` 预处理、`LOAD DATA`、`SELECT ... INTO OUTFILE`、`GROUP BY ... WITH ROLLUP`、多表写（`UPDATE ... JOIN`）、给已有表追加主键、真正的并发与锁、`BACKUP DATABASE`（MySQL 社区版本就没有这条 SQL） |
| **语义差异** | 引擎是 SQLite，不是 InnoDB。字符集/排序规则、`AUTO_INCREMENT` 行为、部分函数返回值等与真 MySQL 有出入。**完整清单在程序内「差异」面板**（源码 `mysql-core.js` 的 `DIFFERENCES` 数组），以它为准 |

> 接手时请牢记这条边界：**这个项目"少做一件事"是设计，不是 bug。** 遇到没实现的功能，正确做法是在 `DIFFERENCES` / `UNSUPPORTED_STATEMENTS` 里如实登记并给错误码，而不是硬造一个假的成功。

---

## 3. 目录与文件地图

```
MySQL模拟器/
├── mysql-terminal.html          ← 【成品】单文件交付件（由 build.py 生成，别手改！）
├── 交接文档.md                   ← 本文档
├── README.md                     ← 面向 GitHub 访客的项目说明（另在开源仓库根目录）
├── .gitignore / .gitattributes   ← 开源仓库的忽略规则与换行符策略（见 §3.1）
├── pack-dist.py                  ← 打包脚本：成品 + README.txt → dist/MySQL终端模拟器-离线单文件版.zip
├── dist/                         ← pack-dist.py 的输出目录（生成物，已被 .gitignore 排除）
│   └── README.txt                       ← 面向普通用户的中文使用说明
├── src/                          ← 【唯一可编辑的源码目录】
│   ├── template.html             ← HTML 骨架 + 5 个注入占位符
│   ├── app.css                   ← 全部样式
│   ├── app.js                    ← 交互层：终端渲染 / 键盘 / 历史 / 补全 / 侧栏（762 行 ≈ 28 KB）
│   ├── mysql-core.js             ← 核心层：方言转译/元数据/排版/错误码/引擎（2458 行 ≈ 128 KB）
│   ├── build.py                  ← 构建：把上面 4 个 + vendor 引擎内联成一个 HTML
│   └── tests/                    ← 全部测试（见 §5）
└── vendor/
    ├── sql-wasm.js               ← sql.js 胶水层（48,788 B）
    └── sql-wasm.wasm             ← SQLite 3.49.1 编译产物（659,806 B，以 base64 内联进成品）
```

> **本仓库已开源**（GitHub 公开仓库）。根目录的 `.gitignore` / `.gitattributes` / `README.md` /
> `THIRD-PARTY-NOTICES.md` 是给公开仓库用的；`dist/` 与测试产生的 `_*_report.txt`、`_shot/`、`_portable/`
> 都已在 `.gitignore` 中排除，不入库。

**铁律：只有 `src/` 下的文件是可编辑的源码。`mysql-terminal.html` 是构建产物，任何手改都会在下一次 `build.py` 时被覆盖。**

---

## 4. 构建流水线

### 4.1 生成成品 HTML

```bash
cd <项目根>/src
python build.py
# → 输出 ../mysql-terminal.html，并打印体积 + 静态检查结果
```

`build.py` 做的事：
1. 读取 `template.html`、`app.css`、`app.js`、`mysql-core.js`、`vendor/sql-wasm.js`；
2. 把 `sql-wasm.wasm` 做 **base64** 编码，塞进 `window.__SQLJS_WASM_B64`；
3. 用 5 个占位符完成替换 —— `/*__CSS__*/`、`/*__SQLJS__*/`、`__WASM__`、`/*__CORE__*/`、`/*__APP__*/`；
4. 对每段 JS 做 `safe_script`（把 `</script>` 转义成 `<\/script>`，防止脚本被提前截断）；
5. **静态检查**：正则扫描 `src=|href=` 的外部 URL 与 `@import`，确认零外部资源。

> 占位符缺一个，`build.py` 会直接退出报错——这是有意的防呆。
> `build.py` 还会 `assert 'wasmBinary' in glue`，确保 sql.js 胶水层支持用内存里的 wasm（而非按路径 fetch 文件）。

### 4.2 打包交付件

```bash
cd <项目根>
python pack-dist.py
# → dist/MySQL终端模拟器-离线单文件版.zip（内含 mysql-terminal.html + README.txt）
```

`pack-dist.py` 会把当前 HTML 的 **大小与 SHA-256 动态写进 README.txt**；`README.txt` 用 **UTF-8 with BOM + CRLF** 写盘（保证 Windows 记事本打开中文不乱码）。

> ⚠️ **改完 HTML 一定要重跑 `pack-dist.py`**，否则 README 里印的指纹会跟实际文件对不上，用户用 `certutil` 校验会提示不一致。

---

## 5. 测试体系与当前基线

测试分两类：**Node 脚本**（无 DOM，直接跑核心层）和 **Playwright 脚本**（真浏览器，跑完整交互）。

### 5.1 Node 侧（`*.cjs`，核心层 / 判定逻辑）

| 文件 | 覆盖内容 | 当前结果 |
|---|---|---|
| `test-core.cjs` | 核心层全量：切分/转译/元数据/排版/错误码 | ✅ 全通过 |
| `test-view.cjs` | VIEW 全链路（建/改/删/SHOW/DESC/引用校验/副作用） | ✅ 全通过 |
| `test-exercise.cjs` | 24 题 × 各种等价写法 + 反例 + 串台检查 | ✅ 全通过 |
| `probe-gaps.cjs` | 特性缺口普查：预期不支持 vs 意外语法错 | 通过 112 / 预期不支持 13 / **意外 1064 = 0** |
| `probe-engine.cjs` | 引擎（需 `node` 加载 sql.js） | ✅ |
| `probe-view.cjs` | 视图探针 | ✅ |

跑法（Node 用 managed 版本）：

```bash
NODE="<你的 node 可执行文件>"
cd <项目根>/src/tests
NODE_PATH=../.. "$NODE" test-core.cjs      # 报告写到 _test_report.txt
"$NODE" test-view.cjs
"$NODE" test-exercise.cjs
"$NODE" probe-gaps.cjs
```

### 5.2 浏览器侧（`*.py`，Playwright）

| 文件 | 覆盖内容 | 当前结果 |
|---|---|---|
| `test-browser.py` | 端到端交互（终端/侧栏/练习打勾） | ✅ |
| `test-terminal.py` | 终端行为 26 项（输入行固定、clear 不毁历史、重连全重置、边界） | ✅ 全部通过 |
| `test-exercise-ui.py` | 复现"只查两列没打勾"截图场景 | ✅ |
| `test-portable.py` | 可移植性 5 项（ASCII/中文路径、断网、禁 localStorage、老内核） | ✅ 5/5 |
| `test-crossbrowser.py` | chromium / firefox / webkit × 中英路径 | ✅ 6/6 组，各 17/17 |
| `test-extreme.py` | 文件截断/损坏兜底（截 10%、只传一半、头部改 1 字节） | ✅ 均有友好提示，不白屏 |
| `audit-sideeffects.py` | 副作用审计 | ✅ |
| `shot-view.py` | 截图工具 | — |

跑法：

```bash
cd <项目根>/src/tests
python test-terminal.py        # 报告写到 _terminal_report.txt
python test-crossbrowser.py
python test-portable.py
python test-extreme.py
```

> 所有脚本都把报告写成 `_*_report.txt`，**不刷屏**。改完代码先看这几个报告文件确认全绿。

### 5.3 改动后的标准动作（务必遵守）

```
改 src/*.js|css|html
   → python src/build.py            （重建成品）
   → 跑 Node 测试 + Playwright 测试  （确认全绿）
   → python pack-dist.py            （重新打包，刷新 README 指纹）
```

---

## 6. 架构说明

### 6.1 四层结构

```
┌─────────────────────────────────────────────┐
│ template.html   骨架 + 启动自检守卫           │
├─────────────────────────────────────────────┤
│ app.js          交互层（依赖 DOM、依赖 Core）  │
│                 终端渲染 / 键盘 / 历史 / 补全   │
├─────────────────────────────────────────────┤
│ mysql-core.js   核心层（无 DOM，Node 可跑）    │
│                 方言转译 / 元数据 / 排版 / 错误码│
├─────────────────────────────────────────────┤
│ vendor/sql.js   SQLite 3.49.1 (WASM)         │
└─────────────────────────────────────────────┘
```

核心层**刻意不依赖 DOM**，才能在 Node 里直接 `require` 做回归测试——这是整个测试体系能跑起来的前提，接手时不要破坏这条边界。

### 6.2 `mysql-core.js` 章节地图（0–13）

| 节 | 内容 | 关键函数/对象 |
|---|---|---|
| 0 | 常量 | `SERVER_VERSION`(`8.0.36-sim`)、`SYSTEM_DATABASES` |
| 1 | 文本宽度与对齐 | `cpWidth` / `dispWidth`（**正确处理 CJK 全角宽度**，表格对齐靠它） |
| 2 | 语句切分 | `splitStatements`（引号/反引号/`--`/`#`/块注释/`\G` 纵向输出） |
| 3 | 建表语句解析（元数据 catalog） | `parseCreateTable` —— 自己解析 DDL 建列目录，因为 SQLite 拿不到 MySQL 式元数据 |
| 4 | **MySQL → SQLite 方言转译** | `translateStatement`（4.1 表选项尾巴、4.2 KEY/INDEX、4.3 列修饰、4.4 AUTO_INCREMENT、4.5 ENUM/SET/JSON→TEXT、4.6 死逗号清理） |
| 5 | 结果集排版 | `formatTable` / `formatVertical` / `rowsTail`（`N rows in set (0.00 sec)`） |
| 6 | 错误映射 | `mapError` —— 把 SQLite 报错翻成 MySQL 错误码 |
| 7 | MySQL 函数注册 | `registerMySQLFunctions` —— 给 SQLite 补 MySQL 函数（`GROUP_CONCAT`、`DATE_FORMAT`、`IFNULL`…） |
| 8 | 虚拟结果集构造 | 变量查询、`SHOW` 结果的构造 |
| 9 | **引擎** | `Engine` 类（见下） |
| 10 | 示例数据 | `SEED` / `seedAll`（内置几个库和表，供练习） |
| 11 | 练习题 | `EXERCISES`（24 题，三档：基础/进阶/挑战） |
| 12 | 与真实 MySQL 的差异 | `DIFFERENCES`（**诚实清单**，程序「差异」面板直接渲染它） |
| 13 | 导出 | 统一 `return {...}`，同时支持浏览器全局 `MySQLCore` 与 Node `module.exports` |

**`Engine` 类主要方法**（第 9 节）：

- 生命周期：`init`（建系统库）、`run`（跑一段输入）、`runOne`（跑单条语句，**命令分发中枢**）
- 元数据：`databaseNames` / `tableNames` / `viewNames` / `viewExists` / `objectList` / `meta` / `tableExists` / `describeTable` / `viewDef` / `buildViewMeta`
- 变量：`systemVarMap` / `readVar` / `substituteVars` / `handleSet`
- 命令：`handleShow`（`SHOW` 系列）、`banner`
- 输出：`resultBlock` / `errBlock` / `statusText`
- 其他：`findDuplicateValue` / `syncCatalogAfter` / `resolveDb`

### 6.3 `app.js` 分区

| 区块 | 内容 |
|---|---|
| 工具 | `$` / `b64ToU8`（wasm base64 解码）/ `toast` |
| 终端输出 | `line` / `renderTableList` 等 |
| 输入区 | 隐藏 `<input>` 承接键盘 + 中文输入法，视觉上是自绘的 `mysql>` 提示符 |
| 执行 | 拿 `buffer` → `engine.run()` → 渲染 |
| 历史与补全 | ↑↓ 翻历史、Tab 补关键字/表名、Ctrl+C 放弃 |
| 练习题完成度 | `markDone` → 调 `Core.matchExercises` 判分 → 写 localStorage |
| 侧栏 | 四个标签：数据库 / 练习 / 差异 / 帮助 |
| 事件绑定 / 启动 | 按钮、快捷键、引擎初始化 |

### 6.4 关键机制 A：终端回滚（为什么 `clear` 不毁历史）

**需求**：输入行永远固定在底部；`clear` / `Ctrl+L` 只清"当前一屏"，往上滚还能回看旧命令；只有「重新连接」才彻底清空。

**实现**（`app.js` 顶部注释与 `clearScreen` 一带）：

- `clearScreen()` **不再** `innerHTML = ''`，而是插入一个零高度的 `.clr-anchor` 锚点，再插入一个撑满"一屏剩余高度"的 `.clr-spacer` 空白块。
- 旧内容全部留在 DOM 里 → 浏览器向上滚就是回滚缓冲，天然可回看。
- 新输出用 `insertBefore(div, activeSpacer)` 插在空白块**之前** → 观感是"新内容从屏幕顶部往下填、空白留在下方"，与真实终端一致。
- 空白高度用 `getBoundingClientRect()` 计算，**不能用 `offsetTop`**（`.scroll` 没有 `position:relative`，`offsetTop` 会算错）。
- `trimScrollback()` 限制回滚缓冲 **5000 行**，但**永不删到锚点之前**。
- `reconnect()` 是唯一调 `resetScreen()`（真 `innerHTML=''`）的地方，同时清空 `history` / `hIdx` / `draft`。
- ⚠️ **已知设计决策**：「重新连接」**不清空练习进度**（进度在 `localStorage`，key = `mysqlsim.done.v2`）。刷新页面、重连都还在。这是刻意的，README 里也写明了。如果产品上想改成"重连=全清"，只需在 `reconnect()` 里加一句清 `doneSet` + localStorage。

### 6.5 关键机制 B：练习题判定（为什么"只查两列"曾不打勾）

**背景 bug**：用户写了 `select username,email from users;`，第 6 题（"只查询用户名和邮箱两列"）没打勾。

**根因**：旧实现 `norm()` 只做 `toLowerCase()` + 空白折叠，然后**全字符串相等**比较。`select username,email` 与参考答案 `select username, email`（逗号后有空格）不相等 → 判定失败。

**修复**：把判分逻辑从交互层搬到核心层，新增两级规范化 + 匹配函数：

```js
canonicalSQL(sql)         // 一级：去大小写、去多余空格、去逗号/括号两侧空格、
                          //       去反引号、去结尾 ; 和 \G
canonicalSQLRelaxed(sql)  // 二级：在一级基础上再剥离单字母表别名
                          //       (SELECT u.username ... FROM users u ≡ SELECT username ... FROM users)
matchExercises(text, list)// 按「语句」逐条比对（不做子串匹配！），返回命中的题目下标数组
```

**三个必须记住的坑**（都踩过、都已在代码里修好）：

1. **反引号必须在状态机之前剥掉**。`replaceOutsideQuotes` 会把 `` ` `` 当成引号起点，导致反引号永远清不掉。正确顺序：先 `s.replace(/`/g,'')`，再进状态机。
2. **大小写转换必须"跳过引号内"**。直接 `s.toLowerCase()` 会把字符串字面量也小写，造成 `'ABC' ≡ 'abc'` 的误判。必须用 `replaceOutsideQuotes(s, /[A-Z]+/g, m => m.toLowerCase())`。
3. **比较单位是"整条语句"，不是子串**。否则 `SELECT username FROM users` 会被判成含有 `username` 而误命中"只查两列"这题。

判定标准必须是"同一条语句的不同写法"，只忽略：大小写、多余空格、逗号/括号两侧空格、反引号、单字母别名。**列数、条件、表名必须真的对得上。**

回归覆盖：`test-exercise.cjs`（152 种写法变体 + 11 个反例 + 串台检查）、`test-exercise-ui.py`（浏览器复现原截图）。

### 6.6 关键机制 C：视图（VIEW）支持

SQLite 引擎层**本来就能** `CREATE VIEW`，但要让它看起来像 MySQL，得把元数据层全部接上：

- 数据库对象新增 `views: {}`；新增 `viewNames` / `viewExists` / `viewDef` / `buildViewMeta` / `objectList`。
- `Engine.runOne` 增加视图分支：`CREATE [OR REPLACE] VIEW`、`ALTER VIEW`、`DROP VIEW`、以及 `DROP TABLE` 命中视图（→ 1347）。
  - `IF NOT EXISTS` 与 `OR REPLACE` **互斥**（同时出现 → 1064）。
  - 解析时按顶层 `AS` 关键字切开（`topLevelKeywordIndex(trimmed,'AS')`），再从头剥 `IF NOT EXISTS`。
- **关键点：建视图后要主动校验引用**。SQLite 对视图引用的表/列是**惰性校验**（真 MySQL 是建视图时就校验），所以必须建完立刻探一次 `SELECT * FROM v LIMIT 0`：引用不存在 → 报 1146 / 1054，并把半成品视图 DROP 掉，不给用户留一个用不了的视图。
- `handleShow` 覆盖：`SHOW TABLES`（含视图）、`SHOW FULL TABLES`（`Table_type` 列标 `BASE TABLE`/`VIEW`）、`SHOW CREATE VIEW`、`SHOW CREATE TABLE` 作用于视图、`SHOW TABLE STATUS`、`SHOW INDEX FROM 视图`（→ 1347）。
- `mapError` 增加：改视图走 DML 的报错 → 1288 `... is not updatable`；`DROP TABLE` 想删视图 → 1347。
- UI：左侧表列表用 `◇` 标记视图，紫色，显示"视图 · N 列"（`.tblrow.isview .glyph`、`.tblrow .cols.vw`）。

**明确不支持的**：真实 MySQL 允许对"简单可更新视图"直接 `INSERT/UPDATE/DELETE` 并写回基表，本模拟器不支持，报 `ERROR 1288`。这条也写在 README 里。

### 6.7 关键机制 D：离线内联 & 启动自检

- **离线**：`sql-wasm.wasm` 以 base64 塞进 `window.__SQLJS_WASM_B64`，启动时 `initSqlJs({ wasmBinary: b64ToU8(...) })` —— 全程不发起任何网络请求（`test-portable.py` 有"断网运行 + 外部请求 0 条"的断言）。
- **启动自检守卫**：`template.html` 开头有一段刻意做得极小的脚本（放在文件最前面，保证只要文件开头完好就能生效）。它起一个 **15 秒定时器**，若到点 `window.__BOOT_SETTLED` 仍为 false，就把启动遮罩换成友好错误文案（并区分"wasm 数据读到了但没跑完" vs "连 wasm 数据都没读到"），提示文件可能被截断、或浏览器内核太旧。
  - 正常启动流程无论成功失败都会置 `__BOOT_SETTLED = true`。
  - 这段是为了防"文件在传输/下载过程中被截断，用户永远卡在初始化界面却毫无线索"。

### 6.8 明确不支持语句的登记方式

`mysql-core.js` 里有 `UNSUPPORTED_STATEMENTS` 表，每项 4 个字段，最后一个 `code` 指定错误码：

- 需要"诚实的 1064"的（如 `BACKUP` / `RESTORE` / `MYSQLDUMP`）→ `code: 1064` + 附解释；
- 需要"这个功能本模拟器没做"的（如 `PREPARE`、`LOAD DATA`）→ 默认 1235。

---

## 7. 关键设计决策与已知限制（沿用即可，别当 bug 修）

1. **底层是 SQLite 不是 MySQL** —— 一切行为差异以 `DIFFERENCES` 清单为准，程序内可查。
2. **`BACKUP DATABASE` 不做** —— MySQL **社区版本身就没有这条 SQL**，它是企业版（MySQL Enterprise Backup）的商业组件；社区版备份靠命令行 `mysqldump`，不是在 `mysql` 客户端里敲的。模拟器如实报 1064 + 说明，**没有凭空造一条 MySQL 不存在的命令**。
3. **视图不可写**（`INSERT/UPDATE/DELETE` 视图 → 1288）。
4. **重连不清练习进度**（localStorage 持久）。
5. **回滚缓冲上限 5000 行**，超出丢最旧的行。
6. **多表写不支持**（`UPDATE/DELETE ... JOIN` → 1235，建议改写成 `WHERE EXISTS (SELECT 1 ...)`）。
7. **`GROUP BY ... WITH ROLLUP` 不支持**（SQLite 不会自动生成小计行）。
8. **给已有表追加主键不支持**（SQLite 需重建整表）。

---

## 8. 当前状态与验证基线

| 项目 | 状态 |
|---|---|
| 成品 HTML | ✅ 1,102,884 字节，SHA-256 `16378e5c…` |
| 核心层测试 | ✅ 全通过，无未捕获异常 |
| 视图测试 | ✅ 全通过 |
| 练习题判定 | ✅ 全通过（含 24 题 × 变体 + 反例 + 串台） |
| 终端行为 | ✅ 26 项全通过 |
| 可移植性 | ✅ 5/5（含断网、禁 localStorage、老内核友好提示） |
| 跨浏览器 | ✅ chromium / firefox / webkit 各 17/17 |
| 极端情况 | ✅ 截断/损坏均有友好提示，不白屏 |
| 特性缺口普查 | ✅ 通过 112 / 预期不支持 13 / **意外语法错 0** |

---

## 9. 待办与未决事项

以下均为 **MySQL 模拟器本体** 的迭代项，都不是缺陷，按需取舍：

- [ ] 是否把「重新连接」改成**同时清空**练习进度（当前刻意为不一致地保留了）——需产品确认。
- [ ] 视图可写（简单可更新视图写回基表）——工作量较大，当前明确不做。
- [ ] 更多练习题 / 更细的难度分层。
- [ ] 若要发布为在线版，需另配托管（当前定位是「离线单文件」，本身不需要）。

---

## 10. 踩过的坑（接手避雷）

1. **`python-docx` 不适用本项目**，但同类坑：读某些带坏 rels 的 office 文件会崩 —— 本项目不涉及，略过。
2. **Bash 工具下 `/tmp` 不可用于 Python** —— 脚本与临时输出一律写到 Windows 路径下的临时目录（例如 `D:\_tmp\`）。
3. **`Remove-Item` 被 safe-delete 拦截**，`trash` 对**含中文/非 ASCII 的路径**会 aborted。删文件用 `[System.IO.File]::Delete($full)`，删目录用 `[System.IO.Directory]::Delete($path, $true)`。
4. **PowerShell 工具不回显 stdout** —— 需要看输出时写文件再用 Read 读（本项目 Node/Python 脚本都自带写报告文件，天然规避）。
5. **改判定逻辑后，务必同时跑 `test-exercise.cjs` 和 `test-exercise-ui.py`** —— 前者查逻辑，后者查真实浏览器里的打勾行为，两者不可互相替代。
6. **改终端相关代码后，必须跑 `test-terminal.py`** —— 回滚/清屏/重连这三件事很容易回归时被悄悄改坏。

---

## 11. 快速命令速查

```bash
# 构建成品
cd <项目根>/src && python build.py

# 打包交付件（会刷新 README 里的指纹）
cd <项目根> && python pack-dist.py

# Node 核心层测试
NODE="<你的 node 可执行文件>"
cd <项目根>/src/tests
NODE_PATH=../.. "$NODE" test-core.cjs
"$NODE" test-view.cjs
"$NODE" test-exercise.cjs
"$NODE" probe-gaps.cjs

# Playwright 浏览器测试
python test-browser.py
python test-terminal.py
python test-exercise-ui.py
python test-portable.py
python test-crossbrowser.py
python test-extreme.py

# 校验成品指纹（Windows 命令行）
certutil -hashfile mysql-terminal.html SHA256
```

---

## 12. 附录：环境与版本

| 项 | 值 |
|---|---|
| 引擎 | sql.js / SQLite **3.49.1**（WASM，659,806 B） |
| 模拟的 MySQL 版本号 | `8.0.36-sim`（`SERVER_VERSION`） |
| 管理 Node（本机示例） | `<你的 node 路径>`（Node 18+ 均可，用系统自带的也行） |
| 管理 Python venv（本机示例） | `<你的 python 路径>`（需先 `pip install playwright`） |
| 浏览器要求 | Chrome 57+ / Edge 16+ / Firefox 52+ / Safari 11+（双核浏览器需切「极速模式」） |

---

**交接验收建议**：接手后依次执行 §11 的「构建 → 打包 → 全部测试」三条链路，确认输出全绿、且 `pack-dist.py` 打印的 SHA-256 与 §1 一致，即视为环境就绪。
